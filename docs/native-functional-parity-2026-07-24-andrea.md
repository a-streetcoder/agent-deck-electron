# Native functional parity audit — 2026-07-24 — Andrea

> **Owner/scope:** Andrea owns the 24 active P1/P2/P3 rows retained in this register, including warnings/settings integration. The injected-command catalog (CMD-01/02), composer slash universe (CMD-03), MCP HTTP add form (MCP-01), global MCP edit flow (MCP-02), automatic OAuth callback (MCP-03), master pause (MCP-04), and default MCP assignment (MCP-05) are closed. This file is also the canonical shared audit history: it preserves the baseline, method, evidence, corrections, closed and present rows, context, dependencies, validation guidance, and limitations for both owner-scoped backlogs. Ale’s 71 active rows are maintained separately in [`native-functional-parity-2026-07-24-ale.md`](native-functional-parity-2026-07-24-ale.md).

## Baseline and method

This report compares:

- **Native macOS:** clean commit `6ba89a5` in `/Users/andrea/Documents/GitHub/agent-deck`.
- **Electron:** exact committed baseline `e2b64fa` in `/Users/andrea/Documents/GitHub/agent-deck-electron`, including the LOOP-27 implementation from commit `57f7974` and the committed validation support.

Those comparison commits remain the fixed original audit baseline. Later implementations, closures, and validation cited below are post-baseline corrections to this living register; they do not move or redefine that baseline.

The scope is functionality, logic, persistence, safety, runtime behavior, and portable product outcomes. Styling, layout, animation, and pixel polish are excluded. Persisted identity/profile asset management is included; visual rendering and style parity are not. Native source is the product reference, not automatically the required implementation architecture.

The original audit traced source, persistence, launch, runtime, cancellation, failure, watcher, Electron main/preload, packaging, and nearby test paths. Tests were inspected but **not generally executed for the original audit**, so most source-backed “present” findings are not fresh runtime certifications. LOOP-27 was subsequently validated on macOS arm64 with focused real-Pi isolated-worktree coverage; browser review, reload, and merge-suppression coverage; Electron opaque-ID reveal coverage; and relevant unit and safety tests. RES-01/02/03 were subsequently validated with native security tests, resource suites, and packaged Electron CRUD/containment smoke on Linux x64, Windows x64, macOS arm64, and macOS x64; the Windows job failed only after those checks on unrelated Loop tests. SKL-22 was subsequently validated with native managed-repository security, server, real-Pi, renderer/backend, and packaged Electron containment coverage across Linux, Windows, and macOS; Windows failures after those checks were unrelated Loop tests. SKL-02 was subsequently validated for legacy per-path merge and durable recovery behavior across Windows, Linux, and macOS; the Windows native-security job's final failures matched baseline `da22bb8` Loop-only failures after its Rust/native SKL tests passed. SKL-15/16 were subsequently closed against the pinned skill engine 0.1.5 with real-addon local-Git route/store acceptance and renderer interaction coverage on macOS arm64; shared packages exist for Darwin arm64/x64, Linux x64 GNU, and Windows x64 MSVC, but this slice did not execute Windows/Linux runtime coverage. GIT-09 was subsequently validated for session-worktree containment on Windows, Linux, and macOS. SUB-09 was subsequently validated locally with the full workspace suite, 82 pinned real-Pi tests, 37 Rust tests plus formatting and Clippy, native smoke, focused Electron opaque-ID security coverage, and relevant builds. SUB-08 was subsequently validated on macOS arm64 with the full workspace suite, 82 pinned real-Pi tests, focused component/browser E2E coverage, static checks, and direct light-theme visual inspection. Windows/Linux runtime execution was not repeated for this shared Node/React/Pi slice; no Electron main/preload, native-module, packaging, or durable-write surface changed. The full E2E suite was also run but had unrelated existing failures, so this report does not claim that suite passed.

Evidence marked **E** is in `agent-deck-electron`. Evidence marked **N** is in native `agent-deck`. Paths are kept short in the register so each difference remains scannable.

## Status and priority taxonomy

<!-- prettier-ignore -->
| Term           | Meaning                                                                                       |
| -------------- | --------------------------------------------------------------------------------------------- |
| **Present**    | Electron has the corresponding functional behavior.                                           |
| **Missing**    | Electron has no corresponding user behavior.                                                  |
| **Partial**    | A useful path exists, but a distinct behavior, state, or persistence path is absent.          |
| **Divergent**  | Both apps implement the area, but users get materially different behavior.                    |
| **Unsafe**     | The Electron path can violate a write, data, isolation, or release boundary.                  |
| **Unverified** | Source does not establish safe parity; focused validation is still required.                  |
| **Decision**   | Product/privacy policy must decide whether this should be ported.                             |
| **Equivalent** | The implementation differs, but the portable outcome is materially the same. It is not a gap. |

<!-- prettier-ignore -->
| Priority | Meaning                                                                                          |
| -------- | ------------------------------------------------------------------------------------------------ |
| **P0**   | Release/parity blocker. No currently registered gap remains at this priority.                    |
| **P1**   | Data-loss/write-boundary risk, isolation/release safety issue, or missing load-bearing workflow. |
| **P2**   | Material limitation with a narrower usable path or workaround.                                   |
| **P3**   | Lower-frequency depth, convenience, diagnostics, or a product decision.                          |

## Executive verdict

Electron has a real and broad cross-platform product. Core Pi sessions stream incrementally. Resource CRUD, provider login, worktrees, Git, memory, MCP, issue browsing, terminal, browser, preview, files, diffs, onboarding, notifications, and signed/notarized macOS packaging all have working source paths.

It is **not at full native functional parity**.

- **No remaining P0 Loop structure gap:** Maker+Checker, Agent Pipeline, report-only Parallel Agents, Discovery/Triage, and terminal Human Approval checkpoints now have native-flat authoring and dedicated orchestration. Human Approval records and resolves durable app-data checkpoints without allocating Pi, validation, checkout-lock, or worktree resources.
- **P1 safety posture:** legacy copied-skill updates now three-way merge the complete payload tree and require per-path choices for overlapping changes; there is no coarse whole-folder remote action. Their durable transaction/recovery path, resource catalog mutations, engine-backed Git collections and per-file stale-review guard, requested project-session worktree isolation, and release synchronization fail closed when their safety boundary cannot be established.
- **P1 workflow gaps:** transcript attachment fidelity remains incomplete outside the now-closed stable-entry fork/edit/rerun path. Generic child runs now have first-class durable records with restart recovery, parent-owned reopenable read-only transcripts, same-child continuation through opaque Pi resume handles, app-owned artifact evidence, caller-bounded parallel concurrency, and optional retained per-child worktree isolation for `managed_parallel`. They intentionally remain owned by the parent rather than becoming top-level sidebar chats. Loop run timelines, proven launch ownership, and validated retained-worktree review are present; apply/discard decisions are implemented and covered by focused server/E2E tests. Running-turn guidance/follow-up input is present; arbitrary crash-orphan process discovery remains unavailable.
- **Distribution is only partly cross-platform:** the macOS DMG and bundled Pi are real. App updates, independent Pi updates, and Windows/Linux release/sign/update pipelines remain absent.

The honest release sequence is: close the remaining data and release hazards, extend durable orchestration/session records, then complete portable update and catalog depth.

## Gaps closed or aligned since older audits

These are post-baseline closures and corrections from later Electron work; they do not change the fixed comparison commits and must not be carried forward as gaps from older audits.

<!-- prettier-ignore -->
| Alignment now present                                                                                                                                  | What changed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **[SUPERSEDED 2026-07-27 — see Corrections]** ~~Project-local authored agent, skill, and prompt catalogs are intentionally unsupported on both sides~~ | **INCORRECT.** Native `PiScanner.swift` DOES scan project catalogs (`.pi/{skills,agents,prompts}` scope `.project`, `.agents` scope `.legacyProject`); the electron rejection was a **regression**, not alignment. Display parity is restored; project skill writes now use the shared engine and round-trip (P3 done). Project agent/prompt writes remain incomplete.                                                                                                                                                 | **E:** `packages/resources/src/paths.ts`, `scanner.ts`; `EngineSkillStore`, resource routes, and focused scope/store tests. **N:** `PiScanner.swift`, `AgentPersistence.swift`, `SkillManagementViews.swift`, `PromptsViews.swift`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Resource catalog mutation containment                                                                                                                  | The pinned Rust capability boundary now provides no-follow resource reads, writes, renames, copies, and deletes, with safe typed failures and mutation serialization rather than a Node filesystem fallback.                                                                                                                                                                                                                                                                                                           | Linux x64, Windows x64, macOS arm64, and macOS x64 native security, resource, and packaged Electron HTTP CRUD/containment validation; **E:** `loop-catalog-native`, `writer.ts`, resource routes/UI, focused unit/server/E2E tests.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Managed skill collection containment                                                                                                                   | The legacy collection implementation's clone, inspect, update, snapshot, and delete paths ran through a pinned native capability rooted at the validated app-data directory. That historical containment validation remains evidence for the retired implementation; current collections are engine-backed as described below.                                                                                                                                                                                         | CI [run 30184425239](https://github.com/a-streetcoder/agent-deck-electron/actions/runs/30184425239) passed all Windows SKL suites, including Rust, native, managed-security, and packaged smoke; Linux/macOS native security passed. **E (historical path):** `loop-catalog-native`, `skillRepositories.ts`, collection server/routes, focused server/real-Pi/E2E tests.                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Session worktree deletion containment and branch cleanup                                                                                               | A held native session-worktree root binds each generated leaf to durable identity. Delete re-proves project, exact Git registration/repository/branch, duplicate ownership, and native identity; after native removal it compare-and-swap deletes only the expected owned branch ref before pruning. The UI explicitly warns that the isolated worktree, branch, and unmerged commits are permanently removed. Branch/CAS/prune failures retain registration and metadata for crash-safe retry; absent registration plus a live branch fails closed, while already-absent branch and registration finish non-destructively. | **GIT-06 closed.** Local macOS arm64 acceptance passed full `pnpm test`, all 75 server and 8 pi-host pinned real-Pi tests, 68 focused native/real-Git tests, focused session-worktree E2E within a broader 221-pass/14-skip run, and static/design checks; remaining broader failures were known unrelated Pi-version and terminal-shortcut cases. Independent review found no material issue. CI [run 30771433278](https://github.com/a-streetcoder/agent-deck-electron/actions/runs/30771433278) subsequently passed the unit matrix on Linux, Windows, and macOS; native security/package smoke on all four target platform/architecture jobs; Linux and Windows E2E; and Linux/macOS real-Pi suites. Its Windows real-Pi job passed `session-worktree.pi.test.ts` before failing only when the unrelated compact regression hit Vitest's outer 60-second timeout, so the overall run is not recorded as green. **E:** `git.ts`, session routes/manager, session worktree route/safety and Git tests, `SessionsPanel.tsx`, focused E2E. **N:** worktree service and delete confirmation. |
| Session worktree merge safety and outcome alignment                                                                                                    | Merge proves persisted/native worktree identity, exact Git registration/repository, and parent/source branches before mutation, under a canonical lock. Dirty or operation-busy parents, missing/occupied sources, and ownership drift fail closed. Typed responses distinguish merged, nothing, dirty, conflict, active-merge, and generic failures; client parsing rejects malformed envelopes, gives actionable recovery, and clears diff caches after auto-commit.                                                 | Local validation passed 1,051 tests, 79 pinned real-Pi tests, 3 focused E2E tests, static checks, and the build. CI [run 30190737390](https://github.com/a-streetcoder/agent-deck-electron/actions/runs/30190737390) had full Ubuntu success and passed the relevant Windows and macOS suites; only unrelated Loop/release flakes remained. **E:** `git.ts`, session merge route/tests, `wsBridge.ts`, focused client tests.                                                                                                                                                                                                                                                                                                                                                                        |
| Parent user decisions                                                                                                                                  | Parent sessions now expose a parent-only `ask_user` bridge tool with structured, accessible decision cards. Concurrent requests have independent exactly-once answer, cancellation, timeout, disconnect, session-removal, and shutdown settlement; resolved cards remain in the transcript as an audit record, and child bridges cannot receive the tool.                                                                                                                                                              | Local validation passed 1,060 tests, 81 pinned real-Pi tests, 5 E2E tests, static checks, and the build. CI [run 30192303484](https://github.com/a-streetcoder/agent-deck-electron/actions/runs/30192303484) passed SUB-01 unit coverage on macOS, Linux, and Windows and real-Pi coverage on macOS and Linux; broader failures were outside SUB-01. **E:** `askUserBridgeTool.ts`, `askUserCoordinator.ts`, bridge/session routes, transcript reducer, `AskUserDecisionCard.tsx`, focused unit/real-Pi/E2E tests. **N:** `PiNativeSubagentBridgeExtensions.swift`.                                                                                                                                                                                                                                 |
| Managed-subagent continuation                                                                                                                          | Completed, failed, stopped, or interrupted single-child runs persist an opaque Pi session resume handle and can continue by stable Deck run ID under the same parent. Continuation reuses the durable run/card identity, preserves streamed ordering and prior agent configuration by default, and rejects active, parallel, legacy, cross-parent, malformed, unsafe-path, or concurrently claimed runs. Child history is parent-owned, reopenable, and inspectable.                                                   | Local acceptance passed the full workspace suite, 82 pinned real-Pi tests, focused continuation Playwright coverage, static/design-system checks, and native/web/backend builds; the full E2E run passed SUB-05 while retaining one unrelated existing terminal-shortcut failure. Runtime validation in this slice was macOS arm64; blocking CI retains Linux/Windows unit and real-Pi coverage. **E:** `bridgeTools.ts`, `SessionManager.ts`, `services/sessionManager.ts`, `subagentRunStore.ts`, transcript cards, focused unit/real-Pi/E2E tests. **N:** native bridge/run service.                                                                                                                                                                                                             |
| Managed-subagent artifact ownership                                                                                                                    | Fresh generic child runs allocate an app-owned, identity-bound artifact root before prompting, persist immutable task/system/output evidence, retain continuation turns separately, and validate owned Pi sessions inside that root. Startup reconciliation retains crash-gap evidence; cleanup is ownership-checked and retryable. Renderer reveal uses only an opaque run ID through a narrow preload/main/token-protected backend capability. Legacy external Pi sessions remain continuable and are never deleted. | Local acceptance passed the full workspace suite, 82 pinned real-Pi tests, 37 Rust tests plus formatting and Clippy, expanded native smoke, focused Electron opaque-ID rejection, static/design-system checks, and native/web/backend builds. The full E2E run passed SUB-09 and had three unrelated failures in MCP OAuth assignment, an external Pi-version expectation, and the terminal shortcut. Runtime validation was macOS arm64; Windows junction/reparse and packaged cross-platform validation remain CI responsibilities. **E:** `loop-catalog-native`, `subagentRunStore.ts`, session manager/routes, desktop main/preload, transcript cards, focused native/server/web/real-Pi/Electron tests. **N:** native subagent run service.                                                    |
| Project-scoped MCP trust and resolution                                                                                                                | Projects now persist an explicit MCP server assignment set. Global and project `.pi/mcp.json` definitions stay inert until assigned; named-agent allowlists can only narrow that project grant. Effective catalogs resolve global, project, then environment definitions by ID, while project-scoped clients, OAuth identity, dispatch, reload, and cleanup prevent same-name servers from crossing concurrent sessions. Malformed config fails closed and preserves the affected project's valid live clients.        | Local validation passed the full workspace suite, 81 pinned real-Pi tests, 5 focused MCP E2E tests, static/design-system checks, and web/backend/native builds. A concurrent real-Pi test proved same-ID project isolation and multiple ordered deltas before finalization. The full E2E run passed all affected MCP coverage; three unrelated existing failures remained in Loop focus, Pi-version expectation, and terminal shortcut coverage. Source and portable Node-path tests cover the shared macOS/Windows/Linux implementation; runtime validation in this slice was macOS arm64. **E:** project contracts/routes, `server.ts`, `mcpTools.ts`, session routes, `McpScreen.tsx`, focused unit/real-Pi/E2E tests. **N:** `MCPConfigLoaderTests.swift`, `MCPAssignmentTests.swift`.          |
| Running-session guidance and follow-up input                                                                                                           | While Pi is running, the composer offers Guide now or Follow up through the prompt `streamingBehavior` path. Pi's bounded authoritative steering/follow-up queue replaces local state in order across replay or snapshot reconnects; correlated acknowledgements retain drafts, contexts, and images safely on rejection or ambiguity, running image submission fails closed, and Stop remains a separate action.                                                                                                      | Local validation passed 1,071 tests, 82 pinned real-Pi tests, 2 E2E tests, static checks, and the build. CI [run 30196013854](https://github.com/a-streetcoder/agent-deck-electron/actions/runs/30196013854) passed macOS and Ubuntu; Windows passed the relevant SES-01/SES-02 checks. **E:** `Composer.tsx`, `composerSubmission.ts`, `wsBridge.ts`, `rpcHandler.ts`, `piHost.ts`, `PiSession.ts`, transcript queue ingest/reducer, focused unit/real-Pi/E2E tests. **N:** `PiAgentRunnerService.swift`, `PiAgentComposerViews.swift`.                                                                                                                                                                                                                                                            |
| Per-session composer draft ownership                                                                                                                   | Unsent text and pending images now remain with the exact chat where they were authored for the renderer lifetime. Session changes render only the new owner’s draft, prune abandoned whitespace-only entries, close transient image previews, and remove deleted-session state. Issue prompts bind to the exact successfully activated session across supersession and list-refresh failure; the no-session transition cannot accept an unowned image.                                                                 | Local validation passed 135 web tests, static checks, the production build, pinned real-Pi tests, and repeated focused Playwright coverage for text/image restoration and image preview. The broader E2E run also passed the new SES-03 scenario; unrelated repository/runtime failures included the local Pi 0.82.1 mismatch against the suite’s pinned 0.82.0 expectation. This renderer-only path uses no platform-specific APIs; fresh Windows/Linux runtime acceptance was not run. **E:** `Composer.tsx`, `store.ts`, `composerSubmission.ts`, `IssuesScreen.tsx`, `wsBridge.ts`, focused unit/E2E tests. **N:** `PiAgentSessionStore.swift`, `PiAgentComposerViews.swift`.                                                                                                                   |
| Durable sent-image transcript persistence and rendering                                                                                                | Sent prompt images enter a strict app-data content-addressed store while transcripts carry only opaque references, never base64 over replay or snapshot WebSockets. Pi-entry identity reconciles images across resume, fork, and delete lifecycle paths; hardened tokenized reads stay lazy, same-origin, and non-enumerable. Historical galleries and dialogs retain accessible keyboard and failure behavior.                                                                                                        | Local validation passed the full 1,064-test workspace suite, 33 Rust tests, 82 pinned real-Pi tests, focused E2E coverage, and packaged smoke. CI [run 30200818721](https://github.com/a-streetcoder/agent-deck-electron/actions/runs/30200818721) passed the relevant Windows SES-04/SES-05 checks and Linux/macOS validation; Ubuntu also completed the full suite successfully in [run 30199911584](https://github.com/a-streetcoder/agent-deck-electron/actions/runs/30199911584). **E:** `sessionImages.ts`, session/RPC/WebSocket lifecycle paths, transcript protocol/ingest, `cells.tsx`, `ExpandedImageDialog.tsx`, focused server/client/web/E2E tests. **N:** `PiAgentRunnerService.swift`, `PiAgentSessionModels.swift`, `PiAgentTranscriptViews.swift`.                                |
| Loop catalog definition containment                                                                                                                    | The pinned Rust capability boundary now provides no-follow, atomic Loop catalog CRUD without a Node filesystem fallback. Registered Loop worktrees remain review evidence rather than entering an automatic deletion boundary.                                                                                                                                                                                                                                                                                         | Blocking [run 30137048096](https://github.com/a-streetcoder/agent-deck-electron/actions/runs/30137048096) passed Linux x64, Windows x64, macOS arm64, and macOS x64 Rust security tests, addon build/load, resource and focused server tests, full builds, and packaged Electron HTTP CRUD/containment smoke.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Managed agent avatar import and storage                                                                                                                | Browser and Electron users can import, replace, and remove PNG/JPEG/GIF/WebP avatars through an accessible hidden file input. Strict shared signature/dimension validation and a 15 MB decoded cap precede atomic app-data blob/manifest writes; no source path is retained or returned. Opaque same-origin URLs render as circular cover images at every shared `AgentAvatar` call site and fall back to the source glyph on load failure. Assignment identity is scope + stable project id where applicable + normalized name, moves on rename, and is removed only with that exact agent on delete/reset. Agent definition and bundled-resource bytes remain untouched. | **AGT-03/04 closed. E:** `agentAvatars.ts`, resource avatar routes/catalog enrichment, `AgentAvatar.tsx`, `AgentsScreen.tsx`, domain metadata, focused store/route/component tests. Local macOS arm64 acceptance passed the full workspace suite, including all 797 server tests (3 skipped), all 278 web tests, 39 Rust tests, and the remaining package suites; focused restart/replace/invalid/path/identity/render/action, route-local >1 MiB import, corrupt-manifest retention/startup, hand-authored-name, rename, and exact cross-scope lifecycle coverage; native addon build/smoke; workspace typecheck; lint/design-system/format; and web/backend builds. The focused browser import/replace/reload/remove scenario passed; it also passed in a broader Playwright invocation that later hit unrelated existing checkpoint and Electron failures and timed out during teardown, so the full E2E suite is not claimed. No Electron main/preload/native-module/packaging path changed; Windows/Linux runtime were not run. Avatar blobs are currently device-local; assignment/blob sync remains an explicit future decision in `sync-seams.md`. |
| Native-calibrated semantic memory qualification and abstention | Semantic recall now embeds title, summary, and the trimmed bounded body prefix; ranks with document-centroid cosine plus discriminative curated-field overlap produced by native’s dedicated semantic tokenizer; requires native-equivalent lexical corroboration for strong, weak, and single-document matches; excludes corpus-common terms only past native’s exact document-frequency boundaries; and applies the qualified-score floor and relative keep cutoff. Native’s 0.02 score buckets precede pin/recency tie-breaking without adding metadata to relevance; `useCount` remains unavailable and tracked separately by MEM-08. Malformed vectors safely use the existing lexical fallback, while ambiguous centered geometry abstains. Pure lexical/fuzzy search keeps its existing tokenizer and behavior. | **MEM-01/02 closed. E:** `packages/memory/src/{semantic,store,text,index}.ts`, table-calibrated memory and server semantic tests. Local macOS arm64 acceptance passed the full workspace suite (1,712 Vitest tests passed, 8 skipped, plus all 39 Rust tests), all 108 pinned real-Pi tests, native addon build/verification and smoke with the pinned Rust 1.88.0 toolchain, workspace typecheck, lint/design-system, repository format check, and web/backend production builds. No renderer, preload, Electron-main, packaging, or platform-specific implementation path changed; E2E, packaged Electron, and Windows/Linux runtime were not run. **N:** `AgentMemoryStore.swift`, `AgentMemoryEmbedder.swift`, `MemoryRecallCalibrationTests.swift`. |
| Explicit global semantic-recall request preference | A persisted global preference now requests semantic ranking across Memory search, bridge-tool recall, and the next turn of already-running sessions without restarting Pi. The legacy environment flag eagerly creates a durable preference only for a missing file or a valid settings object where the field is absent; corrupt, non-object, and malformed-field evidence remains untouched and fails closed, while explicit false remains authoritative. An injected or optional on-device embedder supplies implementation only and is cached across disable/re-enable. The Memory screen exposes an accessible busy/rollback/error switch and an explicit Requested/Not requested preference state, including with no project selected. | **MEM-03 closed.** Final local macOS arm64 acceptance passed the clean full workspace suite: 1,730 Vitest tests passed and 8 skipped, plus all 39 Rust tests; the server contributed 840 passed/5 skipped and the web renderer 300 passed. All 108 pinned real-Pi tests passed (99 server and 9 pi-host), including real memory tool recall, `before_agent_start` recall, prompt-audit chaining, bridge dispatch, incremental streaming, and clean process lifecycle coverage. Acceptance also passed native addon build/verification and smoke, workspace typecheck, lint/design-system, repository formatting and diff checks, and web/backend production builds. Focused coverage includes `semantic-memory-settings.test.ts` (10), persistence byte stability/fail-closed evidence, route validation, same-server injected-embedder toggling, and bridge-tool dispatch through the shared recall function. The first concurrent workspace attempt had one unrelated `sessionManager.test.ts` timing timeout; that file passed immediately in isolation and the complete workspace rerun passed cleanly. The real optional embedder dependency, E2E, packaged Electron, and Windows/Linux runtime were not run. **E:** `services/persistence.ts`, settings/server/memory routes, `MemoryScreen.tsx`, focused tests, `embedder.ts`, `sync-seams.md`. **N:** memory settings/views. |
| Truthful semantic readiness and recall outcomes | The backend now owns a safe readiness lifecycle (`not_requested`, `not_checked`, `checking`, `ready`, `unavailable`, `error`) and recall mode (`lexical`, `semantic`, `lexical_fallback`). Status GET and preference PATCH stay passive; explicit check or active search initializes the cached optional runtime, concurrent initialization coalesces, and initialization retry can recover a failed check. Missing dependency, initialization, runtime embedding, and malformed-vector failures expose only stable reason codes and safe copy while preserving lexical recall. Runtime failures remain visible until the next successful recall, while disable-during-embed results are discarded in favor of lexical recall. HTTP search, bridge-tool search, and `__recall__` return metadata, including no-project and other early-exit recall paths. The Memory screen presents accessible check/retry, distinguishes preference from readiness, fences stale check/search responses, and explains automatic runtime retry without starting Pi or refreshing resources. | **MEM-04/05 closed. E:** `packages/contracts/src/memory.ts`, `packages/memory/src/store.ts`, `apps/server/src/semanticRecall.ts`, memory/bridge/settings routes and tools, `MemoryScreen.tsx`, focused coordinator/server/bridge/renderer tests. Final local macOS arm64 acceptance passed the serialized full workspace suite with 1,764 Vitest tests passed and 11 skipped, plus all 39 Rust tests; the server contributed 855 passed/8 skipped and the web renderer 313 passed. All 108 pinned real-Pi tests passed (99 server and 9 pi-host), including real memory tool and `before_agent_start` recall plus bridge streaming and clean lifecycle coverage. Acceptance also passed native addon build/verification/smoke, workspace typecheck, lint/design-system, repository format and diff checks, web/backend production builds, and the three-test focused browser Memory workflow. Two preceding concurrent workspace attempts hit the same unrelated 10-second `sessionManager.test.ts` attention timeout; all 72 tests in that file passed immediately in isolation and the serialized full workspace run passed. The real optional embedder dependency, full E2E suite, packaged Electron, and Windows/Linux runtime were not run. No dependency, transcript-card, Electron-main/preload, packaging, or persistence surface changed. |
| Recall transcript evidence and exact memory navigation | Each nonempty automatic recall injects its prompt context once, then appends one canonical custom Pi entry after the user message is persisted and before assistant output. Replay reconstructs that evidence in total order from the active ancestry, including after a fork. The durable card retains only memory ID, type, and title snapshot—never query, body, path, or project payload—and derives project identity from authoritative session metadata. The Memory display filter hides only presentation, not the retained cell. Accessible snapshot buttons atomically switch to the exact historical project and Memory view; the latest matching one-shot request GETs only that memory ID and opens the live edit draft. Rename keeps the historical card snapshot unchanged, while deletion, unavailable projects, repeated clicks, races, project changes, and unmounts cannot fall back by title or open another item. | **MEM-06/07 closed. E:** generated Pi bridge custom-entry append, strict domain ingest/reducer, active-ancestry session replay/live ingestion, `cells.tsx`, transcript visibility projection, typed app-store navigation, `MemoryScreen.tsx`, and focused domain/Pi-host/server/web tests. Final local macOS arm64 acceptance passed the serialized full workspace suite with 1,784 Vitest tests passed and 11 skipped, plus all 39 Rust tests; the server contributed 859 passed/8 skipped and the web renderer 323 passed. All 109 pinned real-Pi tests passed (99 server and 10 pi-host), including the focused real-Pi ordering test, incremental streaming, replay/session lifecycle, and clean shutdown coverage. Acceptance also passed native addon build/verification/smoke, workspace typecheck, lint/design-system, repository format and diff checks, web/backend production builds, and a focused three-file renderer run with 52 tests covering the recall card, exact project/ID navigation, rename, deleted/unavailable fallback, repeated-click races, and request ownership. The browser E2E harness has Memory CRUD coverage but no fixture that can inject a Pi custom recall entry into a transcript, so this slice records focused component coverage rather than inventing a browser result; the full E2E suite, packaged Electron, and Windows/Linux runtime were not run. The first concurrent workspace run hit unrelated 5-second avatar/Git timing timeouts; a first serialized retry was interrupted by a 226-second host scheduling pause in an otherwise 1-second native Git test. The final serialized workspace rerun passed cleanly. A first real-Pi run exposed that Pi's `turn_start` can precede user-entry persistence; the append boundary was corrected to the first assistant `message_start` and the focused test plus final pi-host suite passed. A later root real-Pi attempt had an unrelated ask-user timeout during another 143-second host pause; that test passed immediately in isolation, the complete 99-test server real-Pi rerun passed, and the final root run passed all 109 tests cleanly. The custom entry is canonical payload-free evidence: no memory bodies are persisted in transcript history. Fork replay follows Pi’s active ancestry rather than scanning abandoned branches. Legacy sessions without authoritative project identity still receive recall prompt context but intentionally do not create a non-navigable card. |
| Global memory-automation pause and capability status | A persisted default-on preference now pauses automatic recall and all model-facing parent memory tools across every project without deleting, editing, or hiding the stored Memory library or changing the independent semantic-ranking preference. `/settings` separately reports the non-persisted hard server capability, so `AGENT_DECK_MEMORY=0` remains authoritative and the UI shows an unavailable disabled control with server-configuration guidance instead of claiming that a preference can enable memory or that its stored library is available. New launches omit memory policy/index, tools, and recall hook; live guards apply immediately during deferred RES-12 replacement, and idle/parked parents adopt the changed fingerprint while retaining canonical history. Resume restores the same resources. Paused launches pass no explicit append override, leaving ordinary `APPEND_SYSTEM.md` discovery to Pi; focused pinned-Pi coverage proves a trusted project append remains. At that slice, subagents remained unchanged and memory-free; MEM-18 was closed later by the bounded opt-in row below. | **MEM-15 closed. E:** settings persistence/capability routes, effective `/runtime/bridges` inventory, live memory-tool and `__recall__` guards (including pause-during-ranking result discard), compatibility-stable launch resolution/server bridge generation, RES-12 coordination, `MemoryScreen.tsx`, focused persistence/route/tool/recall/fingerprint/resource-refresh/capability/renderer tests, and pinned real-Pi pause/resume replacement coverage. Final local macOS arm64 acceptance passed the serialized full workspace suite: 1,859 Vitest tests passed and 11 skipped, plus all 39 Rust tests (1,898 passing tests total). The server contributed 891 passed/8 skipped and the renderer 341 passed. All 110 pinned real-Pi tests passed (100 server and 10 pi-host), including memory pause/resume replacement, ordinary project/global `APPEND_SYSTEM.md`, tool/recall behavior, incremental streaming, restart, cancellation, and clean lifecycle coverage. Workspace typecheck, lint/design-system, repository format and diff checks, web/backend production builds, and native addon build/verification/smoke passed. The focused browser pause/reload/library workflow had already passed and was not rerun in final acceptance. The first native build invocation could not find Cargo in the ambient shell; rerunning with rustup's `$HOME/.cargo/bin` on `PATH` passed build, architecture verification, and smoke, and the same gate passed again as the real-Pi pretest. Validation used the available Node 26.5.0 rather than the documented Node 22.19 baseline. Full browser/Electron E2E, packaged Electron, and Windows/Linux runtime were not run. No preload, Electron-main, native-module, dependency, packaging, or subagent path changed. The replacement cannot remove memory context already sent before an authoritative idle replacement. |
| Bounded parent recall and native-default delegated-agent memory | A persisted grapheme budget with a 1,000–20,000 user control (default 6,000) now hard-bounds final automatic parent recall and model-facing memory-search output using `Intl.Segmenter`; parent recall/card metadata support five hits and index summaries remain independently capped at 110 graphemes. Named managed children receive up to four task-relevant memories under `min(configured, 3,500)`, resolved only from the authoritative registered parent project and rechecked after ranking and immediately before spawn against capability, master pause, preference, and project identity. Automatic child context defaults on, matching native. Native policy, a 15-entry index, and bounded task recall are appended only to the temporary launch prompt after durable artifact preparation. Independently, ordinary children receive project-authorized memory write/search/mark-stale tools whenever master memory is effective—even when automatic child context is off—plus `contact_supervisor`; restrictive named-agent allowlists gain exactly those generated tools. Child tokens are purpose-scoped, child memory ownership always resolves through the authoritative registered parent project rather than child cwd/worktree, and disposal removes every authorization. Constrained/Loop paths remain memory-free. Source-agent provenance remains separately open under MEM-11. | **MEM-17/18 closed. E:** shared grapheme helpers/preamble/index, settings persistence/routes, live parent recall/tool rendering, generated recall metadata validator, server-composed child callback and generic managed-child launch seam, Memory screen controls, focused tests. Final local macOS arm64 acceptance passed the serialized full workspace suite with 1,886 Vitest tests passed and 11 skipped, plus all 39 Rust tests; the server contributed 908 passed/8 skipped, the renderer 345 passed, memory 62 passed, domain 122 passed, and Pi-host 93 passed. The dedicated 16-test pinned-real-Pi named-child suite passed, covering native-default policy/tools, explicit context-off tools-only behavior, bounded relevant policy/index/recall, and master-paused children receiving neither context nor memory tools. In the broader pinned server-Pi run, 101 tests passed and one skipped before unrelated host stalls affected release setup and one parked-resource test; those two files then passed all eight tests immediately in isolation. Workspace typecheck, lint/design-system, repository format and diff checks, web/backend production builds, and native addon build/verification/smoke passed. Focused fake coverage proves exact bounded attribution, stale/deferred gates, continuation/constrained behavior, canonical parent-project ownership, purpose-scoped child dispatch, authorization cleanup, and durable artifact exclusion. Electron E2E, packaged Electron, and Windows/Linux runtime were not run. No Electron main/preload, native module, dependency, packaging, or resource-fingerprint path changed. |
| Writable Agent Library                                                                                                                                 | Electron now scans and writes `agent-library/agents`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **E:** `paths.ts`, `scanner.ts`, `writer.ts`, `AgentsScreen.tsx`. **N:** `AgentPersistence.swift`, `AgentManagementViews.swift`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Writable Prompt Library                                                                                                                                | Electron now scans and writes `prompt-library`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **E:** `paths.ts`, `writer.ts`, `PromptsScreen.tsx`. **N:** `PromptsViews.swift`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Legacy `~/.agents` precedence                                                                                                                          | Existing legacy global agents win over same-name modern global agents, matching native resolution.                                                                                                                                                                                                                                                                                                                                                                                                                     | **E:** `paths.ts`, `resources.ts`, `scanner.test.ts`. **N:** `PiScanner.swift`, `AgentPersistence.swift`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Recursive nested agent discovery                                                                                                                       | Electron now finds nested agent Markdown and excludes `SKILL.md` and files under `skills` directories.                                                                                                                                                                                                                                                                                                                                                                                                                 | **E:** `scanner.ts`, `scanner.test.ts`. **N:** `PiScanner.swift`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Ambiguous mutation conflicts                                                                                                                           | Same-name legacy and modern sources now produce a conflict instead of editing an arbitrary file.                                                                                                                                                                                                                                                                                                                                                                                                                       | **E:** `writer.ts`, `routes/resources.ts`, `editing.test.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Legacy copied-skill merge and recovery safety                                                                                                          | Legacy imports three-way merge the complete payloads: non-overlapping changes apply automatically; overlapping paths require Keep Mine/Take Remote, defaulting to Mine, and the coarse whole-folder remote action is rejected. Durable journals, native atomic replacement, startup rollback/reconciliation, and retained recoveries protect interrupted catalog/settings writes. Electron uses OS Trash; unavailable Trash leaves recovery visible for restore or retry.                                              | CI [run 30223559272](https://github.com/a-streetcoder/agent-deck-electron/actions/runs/30223559272) passed Windows Rust/native SKL tests and the Linux/macOS native jobs; the Windows native-security job's final failures matched baseline `da22bb8` Loop-only failures. **E:** `skillTreeFingerprint.ts`, `writer.ts`, native resource recovery capability, transaction/reconciliation and merge routes, `SkillsScreen.tsx`, Electron Trash bridge, focused resource/server/E2E tests.                                                                                                                                                                                                                                                                                                            |
| Engine-backed per-file collection conflicts                                                                                                            | With pinned `@a-streetcoder/skill-engine-native` 0.1.5, `EngineSkillStore`, resource routes, and Skills UI preserve non-overlapping upstream changes and let each overlapping path choose Keep Mine or Take Remote. A changed conflict presentation rejects its formerly valid merge ID with `409 LEGACY_MERGE_STALE`; refresh supplies a new ID before retry.                                                                                                                                                         | **E:** `skill-git-conflict.acceptance.test.ts` uses the real addon and local Git to prove stale-ID rejection, refresh, mixed choices, non-overlap merge, and exact bytes; `SkillsScreen.test.tsx` proves mixed submission and stale refresh/reset/retry. Full workspace tests passed (server 595 including the new acceptance test; web 192 including the interaction test), as did 82 real-Pi tests and typecheck/lint/design-system/format. Full E2E recorded 216 passed/14 skipped and four unrelated failures (Loop focus, MCP assignment, installed Pi 0.82.1 versus expected 0.82.0, terminal shortcut). Runtime acceptance was macOS arm64 only. The synchronous engine route has no deterministic interruption state to inject; the supported stale refresh/retry recovery path is covered. |
| Nearest-existing-parent watchers                                                                                                                       | Missing catalogs are watched from an existing ancestor without creating catalog directories.                                                                                                                                                                                                                                                                                                                                                                                                                           | **E:** `watcher.ts`, `paths.ts`, `watcher.test.ts`. **N:** `AppRefreshService.swift`, `AppViewModel.swift`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Native builtin override scope                                                                                                                          | Native builtin overrides are global-only. Per-project builtin-agent disable is therefore removed from the gap list.                                                                                                                                                                                                                                                                                                                                                                                                    | **N:** `AgentPersistence.swift`, `BuiltinOverrideDisabledTests.swift`. **E:** `overrides.ts`, `routes/resources.ts`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Builtin-to-custom agent replacement                                                                                                                    | A pristine or globally overridden effective builtin offers one Replacement action that opens the existing custom-agent editor with editable identity, fixed global scope, effective managed values, prompt body, and unknown override fields. Save uses the existing agent-create mutation and inline writer with an atomic create-only claim; bundled and settings bytes remain unchanged, while cancel writes nothing. Narrow layouts stack catalog/detail to keep the action and explanation readable. | **AGT-11 closed. E:** `AgentsScreen.tsx`, `AgentEditSheet.tsx`, `agentReplacement.ts`, existing resource PUT/writer, focused resource/server/component/Playwright tests. Acceptance passed the full workspace suite, 85 pinned real-Pi tests, static/design checks, native/web/backend builds, and both AGT-11 E2E scenarios. Full E2E recorded 225 passed/14 skipped with only the unrelated installed Pi 0.83.0 versus expected 0.82.0 check and terminal shortcut failure. Direct light/dark inspection at 1280×800 and 760×700 covered pristine/overridden detail and editor focus without clipping after the narrow-layout correction. Runtime validation was macOS arm64; existing native containment/create-only tests are portable, while Windows junction/reparse and Linux runtime were not run. No new store or mutation seam was added. |

### RES-12 — running-session resource refresh

Ordinary plain and named parent sessions now persist a bounded semantic launch-resource configuration and fingerprint. A resource-change signal coalesces current catalog, settings/default, provider/model, skill, extension, instruction/context, MCP/bridge-scope, and prompt-template resolution through the same launch resolver used for creation. Active turns defer until authoritative idle; eligible live sessions replace the Pi process only after preflight succeeds, while parked sessions adopt the plan without waking and preflight current MCP resources on their next wake. The replacement preserves the Pi session file and conversation ancestry, cwd and isolated-worktree ownership, explicit non-secret launch overrides, total event ordering, cancellation/failure cleanup, and emits `session_rebind` only after the replacement is authoritative. Worktree creation recomputes cwd-sensitive resources after final allocation and rolls back proven worktree/branch ownership if that final resolution fails.

This is controlled process replacement, not Pi hot reload. Instruction files, extension file bytes, and MCP configuration remain outside automatic file watching as documented in the shared watcher corrections: their semantic changes apply to existing tracked sessions when an explicit `resources_changed` signal occurs (including owned UI refresh/mutations where provided), not merely because external bytes changed. Existing watcher coverage for catalog/settings changes is unchanged. Explicit launch-environment values may contain credentials and therefore never enter public session metadata, persistence, fingerprints, errors, diagnostics, or sync. A private manager map retains only request-supplied values across same-process replacement, idle parking, and ended-session resume, merging them over current environment defaults; destroy and shutdown clear it. App restart intentionally returns to existing current-default behavior because these secrets are not durable. Records created before the typed non-secret launch-resource metadata was introduced are not retroactively guessed or auto-refreshed; their legacy resume behavior remains unchanged. Helpers, managed children, and Loops remain outside RES-12.

Accepted on macOS arm64. Native addon build/verification and smoke passed. The full workspace validation executed 1,676 Vitest tests with 6 skips plus all 39 Rust tests. Both concurrent `pnpm test` attempts passed 1,675 tests and hit the same unrelated existing `sessionManager.test.ts` attention wait timeout; that exact test passed in isolation, and a complete server rerun passed all 815 server tests with 3 skips. All 107 pinned real-Pi tests passed (9 pi-host and 98 server). The RES-12 real-Pi scenarios specifically prove multiple ordered old-process deltas and final/idle before exactly one authoritative rebind, retained conversation history after replacement, and parked adoption followed by current-resource wake without eager spawn. Workspace typecheck, lint/design-system, repository-wide format check, native addon build/smoke, and web/backend production builds passed. Browser and Electron E2E were intentionally skipped because no renderer, preload, Electron-main, or user-visible interaction path changed. Windows/Linux runtime, packaged Electron, and package/release validation were not run. Independent final review reported no blockers. A subsequent pre-commit security correction removed raw request-environment values from the public contract and persistence, added the process-local lifecycle described above, and passed 140 focused server tests plus workspace typecheck and formatting; the broader acceptance evidence otherwise remains the preceding run. **RES-12 closed. E:** `launchResources.ts`, `SessionManager.ts`, session/settings/server wiring, persistence/protocol contracts, `sync-seams.md`, focused server tests, and `extensions-discovery.pi.test.ts`. **N:** native launch resolver/update behavior.

### AGT-07 — custom-agent extension allowlists

Custom and builtin-replacement agent files now preserve the semantic distinction between an absent extension field (use the current enabled catalog), an explicit empty list (load no user extensions), and an ordered explicit file-path allowlist. The editor uses the live extension catalog with provenance plus disabled, missing/non-file, app-bridge-conflict, duplicate-name, loading-mode, and stale-value diagnostics; stale hand-authored values remain removable but fail closed at launch. Builtin extension overrides remain intentionally unsupported. Parent named sessions and fresh, continued, or parallel named managed children resolve the current agent definition against the same current catalog policy; anonymous children are unchanged.

Accepted local macOS arm64 validation passed the native addon build/smoke; the full workspace suite with 1,651 Vitest tests passed and 6 skipped plus all 39 Rust tests; all 98 pinned real-Pi tests; workspace typecheck; lint/design-system/format; and web/backend builds. Real-Pi coverage proves named-parent explicit selection and explicit empty, disabled/loading-mode/bridge-conflict refusal, named-child loading and disabled refusal, named-child continuation, and ordered incremental streaming. Playwright, packaged Electron, and Windows/Linux runtime were not run; no Electron main/preload, native packaging, or sync seam changed. **AGT-07 closed. E:** domain normalization, resource scanner/writer, resource/session routes, named-agent resolver/session manager, `AgentEditSheet.tsx`, `AgentsScreen.tsx`, and focused unit/real-Pi tests. **N:** custom-agent extension picker/persistence behavior.

### AGT-05 — agent identity and configuration profiles

Closed as equivalent after two independent source reviews of the current Electron and native management surfaces. Electron’s sectioned master-detail Agents screen now provides the portable profile outcome: avatar and fallback identity, name/scope/description/provenance, warning state, model/thinking, prompt and mode, tools/direct tools, skills, MCP servers, fallback models, extensions policy, subagent metadata, project assignment/default state, and lifecycle actions. Native presents comparable information in stacked `AppCard`s; differences such as per-card Edit buttons, conditional field chrome, macOS Image Playground generation, collection-member expansion, and the whole-project assignment matrix do not make Electron’s profile surface absent. Edit-time picker validation remains separately tracked by AGT-09.

No code changed for this closure. Existing focused component/browser coverage already exercises profile warnings, metadata, assignments, extension states, avatar rendering/fallback/import lifecycle, replacement state, and detail selection. Independent review found no missing profile-comparison behavior that warranted new UI or persistence work.

### AGT-12/13 — actionable agent warnings and project-aware skill visibility

The agent catalog now carries a typed, bounded, generated-only warning envelope evaluated against the selected project's current skill candidates, disabled-skill settings, and effective Exa environment presence. Rows expose a clear warning count and detail shows an accessible current-project panel for the native warning set: missing skill, Exa tools without `EXA_API_KEY`, `web_fetch` while Exa is configured, and nonempty extensions without explicit ordinary/direct tools. Electron additionally distinguishes disabled and ambiguous assigned skills rather than conflating them with missing names.

The editor's Skills tab retains hand-authored stale names and uses sequenced, abortable current-project candidate loading to explain missing, disabled, ambiguous, global/project visibility, and cross-project bare-name behavior. The candidate scanner honors mandatory ordered standard precedence (`project .agents` > `project .pi` > `global .pi/agent` > global legacy); only true same-priority ties remain ambiguous, without bypassing `EngineSkillStore` or changing storage. Named parent and managed-child launch now share one strict preflight: missing, disabled, or truly ambiguous explicit skills and case-sensitive explicit tool allowlists without `read` fail actionably before Pi allocation; unique skills retain authored order and real injection. Ambient parent defaults continue skipping stale missing/disabled assignments and cannot block a named plan that replaces them. Explicit empty tools survive scan/override/write/launch as Pi `--no-tools`, while absent tools retain Pi defaults. Anonymous children, `--no-skills` helper behavior, and streaming are unchanged.

Accepted local macOS arm64 validation passed the native addon build/smoke; the full workspace gate with 1,664 passing Vitest tests, 6 skipped, and all 39 Rust tests; workspace typecheck, lint/design-system, and format; web and backend production builds; all 105 pinned real-Pi tests with no-spawn named parent/child failures, ordered skill injection, and explicit-empty versus absent tools; workspace route coverage for named and explicit-body skill replacement of ambient assignments; and the focused warning/editor Playwright workflow. The first full `agents.spec.ts` attempts exposed an existing stale expectation that an absent extension field hid the detail card; it was aligned with the existing truthful “Default catalog policy” presentation, while the final focused AGT workflow passed. Full Playwright, Electron, packaged, Windows, and Linux runtime validation were not run. No Electron main/preload, native storage/addon, packaging, persistence, or sync seam changed. **AGT-12/13 closed. E:** domain warning contract, Pi-shaped skill candidate scanner, server catalog/runtime preflight, agent editor/detail, and focused domain/resource/server/web/real-Pi/Playwright tests. **N:** `PiScanner.swift`, `PiSkillVisibilitySmokeTests.swift`, native subagent launch preflight.

### CMD-01/02 — app-owned injected command catalog and per-command enablement

Agent Deck now owns a distinct injected slash-command catalog, separate from renderer commands, prompt/skill commands, user extensions, and model-facing bridge tools. Two bundled workflow commands are materialized from release-owned source into trusted app data and enabled by default; browser/Electron file input imports bounded `.ts`/`.js` text into an app-owned library without retaining the source path, with imports disabled by default. Stable ids persist disabled bundled and enabled library assignments with compatibility sanitization and fail-closed stale ids. The one store seam scans on restart, performs no-follow regular-file reads and atomic app-data writes, revalidates canonical app-owned identity immediately before deleting exact library entries, and rejects malformed/no/multi-command files, managed-name collisions, every dynamic `import(...)` form, alternate/aliased/computed command registration, bridge/tool/hook/provider registration, and statically evident privileged runtime access. Public command DTOs contain no filename or filesystem path. The accessible Commands section groups bundled and imported ownership, exposes slash name/title/provenance/description and independent toggles, warns prominently that enabled imports execute with Pi extension-runtime capabilities, and supports import and imported-byte deletion without displaying filesystem paths.

Enabled files are explicit Pi extensions for ordinary plain and named project parent sessions regardless of user extension loading mode or named-agent user-extension allowlists. They grant no tools, remain excluded from no-project sessions and the separate helper/managed-child/Loop launch paths, continue to rely on Pi's authoritative `get_commands` catalog and command dispatch, and participate in the existing RES-12 fingerprint/rebind path after every mutation. Real-Pi coverage proves the bundled `waitForIdle` → optional-guidance → `sendUserMessage` workflow, multiple ordered deltas before final/idle, bundled disable/rebind removal, disabled-by-default import, imported enable/execution, delete/rebind removal, named-parent injection in managed mode, and no-project exclusion.

This is intentionally stricter than native's current library scanner: native can project multiple literal `registerCommand` calls from one imported file into multiple settings rows while still injecting the shared file, so toggling one row does not truly isolate that command. Electron accepts exactly one literal command per imported file so one toggle owns one extension and cannot accidentally retain sibling commands. The import check is conservative static validation, not a JavaScript parser or security sandbox; imported command code still runs with Pi's extension runtime when explicitly enabled, and unsupported dynamic/privileged extension shapes must remain ordinary user extensions instead. Local macOS arm64 acceptance passed the fresh native-addon build/verification and smoke, all 39 Rust tests, workspace typecheck, lint/design-system/format, web/backend builds, all 108 pinned real-Pi tests, and the focused browser import/toggle/delete workflow. The full workspace run passed 1,688 Vitest tests with 6 skips; one unrelated existing `sessionManager.test.ts` terminal-provider-failure attention wait timed out under the concurrent load, passed immediately in isolation, and the complete server suite then reran green with 824 passed and 3 skipped. Focused coverage includes command persistence/store/launch/routes, eight renderer Commands/Extensions cases, static-import/alternate-registration rejection, path-free API records, catalog response races, and project/named/no-project launch policy. Full Playwright, Windows/Linux runtime, packaged Electron, and release validation were not run; no native-addon, Electron main/preload, packaging, or release surface changed. **CMD-01/02 closed. E:** `injectedCommands.ts`, settings persistence, command routes, `launchResources.ts`, Extensions Commands UI, `sync-seams.md`, and focused store/UI/E2E/real-Pi tests. **N:** `PiInjectedCommandExtensions.swift`.

### GIT-02 — isolated session branch-collision recovery

Isolated session allocation now retries only after `git branch` fails and an exact post-failure `refs/heads/...` lookup proves that candidate exists. It keeps the original reserved worktree leaf and native identity while trying the generated base branch, then `-2` through `-50`; every non-collision Git failure stops immediately, exhaustion retains the typed actionable isolation error, and no path falls back to the primary checkout. Real-Git coverage proves an occupied base allocates and registers `base-2` with exact branch ownership, all 50 occupied candidates exhaust without target or registration residue, and non-collision failures remain non-retryable.

Accepted macOS arm64 validation passed the pinned Rust 1.88 native-addon build/smoke, typecheck, lint/design-system, format, the full workspace suite (including 39 Rust tests, 708 server tests with 3 skipped, and 238 renderer tests), and all pinned real-Pi suites (79 server and 8 pi-host tests, including all 9 session-worktree scenarios). Full Playwright recorded **225 passed/14 skipped/3 failed**: the diff-comment hover timeout passed on focused rerun, while the installed Pi 0.84.1 versus expected 0.82.0 check and the previously documented terminal-shortcut failure were unrelated; both session-worktree E2E scenarios passed. Native, web, and backend builds passed, while the full bundled-runtime build stopped in `build:pi-runtime` when the current credentials could not fetch the private `@a-streetcoder/skill-engine-native` package (registry 404). Windows/Linux runtime execution was not performed. This backend/Git slice changed no UI, persistence, Electron main/preload, packaging, or sync seam. **E:** `git.ts`, session routes, focused route/real-Git tests, and pinned real-Pi session-worktree tests. **N:** `PiAgentSessionWorktreeService.swift` candidate outcome.

### ISS-07 — incomplete GitHub issue results disclosure

The project issue route now asks `gh issue list` for one sentinel row beyond its 50-item display cap, preserves GitHub's returned order, and exposes a typed `incompleteResults` flag only when row 51 exists. The Issues screen shows a polite visible status that truthfully scopes search and label/assignee/author filters to those returned results, clears it before loading or failure, and binds response application to both request and synchronously committed project/state query identity.

Acceptance passed the full workspace suite, typecheck, lint/design-system/format, native addon build/smoke with Rust 1.88, focused issue browser E2E, and web/backend builds. Full E2E recorded 225 passed/14 skipped with three unrelated existing failures: installed Pi 0.83.0 versus the 0.82.0 expectation, large-paste marker cleanup timing, and the terminal shortcut. Directly inspected light/dark Chromium screenshots at 1280×800 and 420×760 covered 51 returned rows, long titles, keyboard-search behavior, responsive controls, notice hierarchy/contrast, and no notice or content overflow; deterministic component tests cover stale state/project responses, loading, and failure. The pure 49/50/51 normalization and `execFile` argv path are shared across macOS, Windows, and Linux, but actual Windows `gh.exe`, Linux runtime, and non-macOS E2E were not run. No persistence, IPC, process-tree, packaging, or sync seam changed.

### SUB-02 — parent pending supervisor-request listing

Parent Pi sessions now receive a strict, read-only `list_supervisor_requests` bridge tool. It returns only the calling parent's runtime-pending child requests with the native portable `requestID`, `kind`, `title`, `message`, and stable Deck `runID` fields; progress, answered, cancelled, and other-parent records stay excluded. Child tool policy and extension-conflict filtering keep the capability parent-only. The existing out-of-band UI/REST answer path remains authoritative: because a synchronous managed-child call normally blocks its parent while waiting, this slice proves the listing contract and transport but does not claim that the same blocked parent can act before separate asynchronous child-lifecycle work.

Acceptance on macOS arm64 passed the full workspace suite (including 697 server tests and 39 Rust tests), typecheck, lint/design-system, format, backend build, focused unit/domain tests, and a pinned real-Pi tool round-trip. The full real-Pi suite passed SUB-02 and 76 other server scenarios but had two unrelated existing timeout failures in a Loop isolated-worktree scenario and non-blocking supervisor progress; the latter passed alongside SUB-02 on a focused rerun. Full Playwright recorded 226 passed/14 skipped with two unrelated existing failures: installed Pi 0.84.1 versus the suite's 0.82.0 expectation and the terminal keyboard shortcut. This server/Pi bridge path has no OS-specific implementation branch; Windows/Linux runtime execution was not repeated. No persistence, renderer, Electron main/preload, packaging, or sync seam changed.

### SUB-03 — parent supervisor-request answering

Parent Pi sessions now receive a strict `answer_supervisor_request` bridge tool that trims the response and owner-scopes pending lookup to the authenticated calling session. It reuses the existing exactly-once coordinator: successful answers update the parent transcript card and runtime log, resolve the suspended child tool call once, and make later, cancelled, stale, or foreign-parent attempts return the same non-disclosing normal-text result as native. Existing UI/REST answering remains unchanged, and child tool filtering plus extension-conflict detection keep this capability parent-only.

Focused acceptance passed 40 server tests, 7 domain tests, and pinned real-Pi coverage for both answer-tool transport and the existing successful child-settlement path, plus typecheck, lint/design-system, format, native addon build/smoke, and backend build. A broad workspace run passed the native, domain, contracts, memory, and MCP suites before one unrelated `pi-host` correlation timeout; that exact seven-test file passed immediately on a focused rerun. Independent review found no implementation or ownership defect. The real-Pi answer-tool scenario necessarily proves registration and stale-result transport rather than a live same-parent answer: ordinary `managed_subagent` and `managed_parallel` calls synchronously block that parent model turn, as they also do in the native reference, so the existing UI/REST path remains the practical unblocker until a future asynchronous child lifecycle exists. This shared Node/Pi bridge path has no OS-specific branch; Windows/Linux runtime execution was not repeated. No persistence, renderer, Electron main/preload, packaging, or sync seam changed.

### SUB-08 — parent-owned child transcript inspection

Every generic child card now opens a parent-owned read-only transcript while running and after completion, failure, stop, interruption, continuation, or restart. Live cells project from the existing single Pi event consumer; terminal and restart reads reconstruct pinned Pi `get_entries` active ancestry. The UUID-scoped parent/run endpoint returns sanitized cells only, rejects foreign pairing, revalidates owned evidence before and after asynchronous reconstruction, and labels retained legacy/external evidence `summary_only`. Parent deletion invalidates live reads and remains the only cleanup transaction.

Acceptance passed `pnpm typecheck`, lint/design-system/format, the full workspace suite (server 602, web 197), all 82 pinned real-Pi tests, and the focused SUB-08 E2E scenario. The updated real-Pi test proves multiple ordered live child updates before finalization alongside ordered parent deltas (one consumer), then canonical user→assistant ancestry. Full E2E recorded 218 passed/14 skipped with two unrelated failures (installed Pi 0.82.1 versus expected 0.82.0; terminal shortcut). Focused unit/component coverage includes malformed/foreign ownership, safe cells-only output, terminal/summary states, deletion races, continuation polling, loading/empty/error/Retry/source status, focus trap/Escape restoration, and overflow. macOS arm64 Chromium visual inspection at 1280×800 light theme covered live/canonical long content; the observed terminal `Live · Done` settlement bug was fixed and regression-tested. Independent review found no remaining material issue. Windows/Linux runtime execution was not performed; the portable path has no platform-specific branch. No packaged smoke applies, and no sync-seams registry update is needed because no durable store/write path was added.

### AGT-01 — default global/library agent availability

Closed as equivalent after rechecking the current native and Electron launch models. Electron’s singular `ProjectMeta.defaultAgentName` is only a per-project preselection for the active named parent session; it is not the counterpart of native’s global `AppSettings.defaultAgentNames`. Native uses that set as an inclusion gate that promotes selected global/library custom agents into each project’s effective catalog alongside builtins. Electron instead scans every unshadowed global/library custom agent into the project catalog and permits any non-disabled result to launch as a named parent or through `managed_subagent`, so users already have the portable outcome of a default team being available without maintaining a second assignment set.

At that point this did not close project-scoped curation: native `ProjectPreferences.assignedAgentNames` was a separate multi-agent concept, and Electron still lacked that narrower assignment set under AGT-02/PRJ-01. Two independent source reviews confirmed that adding a global multi-default setting under AGT-01 would conflate session preselection with catalog inclusion and could regress today’s open availability. No runtime, persistence, UI, sync seam, or test surface changed in that AGT-01 closure.

### AGT-02 / PRJ-01 — project-scoped multi-agent curation

Projects now persist optional `ProjectMeta.assignedAgentNames` independently from the singular active-session `defaultAgentName`. Legacy absence keeps every scanned agent available; after an explicit assignment patch (including `[]`), builtins remain available and only assigned custom names enter project pickers and named launch/delegation resolution. The patch stably deduplicates validated resource names, rename re-points assignment/default references, delete and disable cannot leave stale effective access, and unavailable direct names fail through the same not-found result as unknown names. The Agents detail exposes an accessible multi-assignment toggle and labels the star as the separate active-session default; the Projects recap distinguishes both concepts.

Acceptance on macOS arm64 passed focused contract, server, renderer, and assignment browser E2E tests; all 96 pinned real-Pi tests; native addon build/smoke; full typecheck, lint/design-system, format, and web/backend builds. The real-Pi proof covers ordered named delegation plus unassigned custom direct-launch and managed-delegation refusal. The full workspace suite completed with all affected tests passing but three unrelated server tests timing out under concurrent load (session-attention failure settlement, subagent worktree identity retention, and release-tag rollback); each passed immediately on an isolated rerun. Electron-specific E2E and Windows/Linux runtime execution were not run, so this closure does not claim cross-platform runtime certification. **E:** `ProjectMeta`, agent curation helper, project/resource/session routes and resolver, Agents/Projects screens, focused contract/server/web/browser-E2E/real-Pi tests, `docs/sync-seams.md`. **N:** `ProjectPreferences.assignedAgentNames`, project/agent assignment views.

### SUB-21 / AGT-06 — maximum depth compatibility and complete editable subagent fields

Custom and replacement agents now scan, edit, clear, persist, and display native’s optional non-negative `maxSubagentDepth`. Zero remains distinct from absence; the editor presents native’s usual 0–10 range while preserving valid higher hand-authored values, matching native’s unbounded non-negative parser/writer. Audited/current native has no runtime consumer, so Electron accurately labels it compatibility metadata and retains its stricter existing prohibition on recursive child delegation. Effective builtin integer overrides apply and seed replacements while remaining unmanaged during unrelated builtin edits. With SUB-15 through SUB-21 now represented in the active editor and runtime where native actually consumes them, umbrella AGT-06 is also closed.

Acceptance on macOS arm64 passed 62 focused resource/server/editor/detail tests, typecheck, lint/design-system, and format. Coverage includes blank, zero, ten, higher hand-authored values, negative/fractional API rejection, writer validation, effective override preservation, replacement seeding, dirty/conflict/save behavior, conditional detail display, and accessible compatibility-only help. Real-Pi was not run because no runtime, prompt, process, or Pi launch path changed. Windows/Linux runtime, screenshots, and Electron E2E were not run. No preload/main, native storage, dependency, packaging, or sync seam changed.

### SUB-20 — agent default read-first paths

Custom and replacement agents now scan, edit, clear, persist, and display ordered project-relative `defaultReads`. Named fresh, parallel, and continued children re-resolve the current agent definition, sanitize manually authored defaults entry by entry, then merge defaults before the current call’s strict `reads` with stable deduplication. The effective list is validated against the shared cross-platform path, 32-item, 512-byte-per-path, and 1,102-byte artifact budget before durable identity or Pi spawn, then becomes the child prompt, latest run record, and per-turn `input.md`. Caller reads remain all-or-nothing; prior per-call reads never leak into a continuation; paths remain hints and contents are never preloaded. Authoring rejects a sanitized definition that could never fit the launch budget, while manual-file scanning remains best-effort.

Acceptance on macOS arm64 passed 176 focused domain/resource/server tests plus the pinned real-Pi continuation scenario proving current-definition re-resolution, defaults-first ordering, unsafe-default omission, per-turn durable evidence, stable identity, and multiple ordered deltas. Typecheck, lint/design-system, format, native addon build/smoke, and backend/web builds passed; five focused agent Playwright scenarios had passed before the final validation-only hardening. Broad workspace and real-Pi attempts were heavily CPU-starved by concurrent test runs and produced unrelated timeouts across terminal, Git, Loop, diff, picker, and unknown-agent cases; all SUB-20 focused suites and the affected real-Pi scenario passed immediately in isolated reruns. Windows/Linux runtime and full Electron E2E were not run. No preload/main, native storage, dependency, packaging, or sync seam changed.

### SUB-19 — advisory agent output

Custom and replacement agents now scan, edit, clear, persist, and display native’s optional `output` string. Named fresh and continued children receive the configured value as safely quoted advisory metadata alongside the authoritative SUB-16 outcome contract. The value is single-line and bounded; it does not grant tools, validate or authorize a project path, create a worktree, select app-owned `output.md`, or affect anonymous children. Effective builtin string/false overrides apply and seed replacements while remaining unmanaged during unrelated builtin edits.

Acceptance on macOS arm64 passed the full resource, web, and server suites (172 resource, 273 web, and 778 server tests, with existing skips), focused pinned real-Pi named delegation including continuation and ordered streaming, five focused agent Playwright scenarios, typecheck, lint/design-system, format, and backend/web builds. Independent review found no material issue. The broad real-Pi run passed 85 of 86 before one initial continuation assertion raced idle; the corrected focused continuation test passed. Windows/Linux runtime was not run. The aggregate Pi-runtime build was blocked by unavailable private-package authentication, unrelated to this shared TypeScript/React/Pi slice. No preload/main, native storage, dependency, packaging, or sync seam changed.

### SUB-18 — interactive compatibility metadata

Custom and replacement agents now scan, edit, persist, clear, and display native’s `interactive` boolean. Audited/current native code treats the field as compatibility metadata only, so Electron does not claim it enables prompts or changes child behavior. Native-compatible persistence writes only `true`; false/absence resolves and displays as Interactive Metadata: No. Effective builtin overrides apply true or false, remain unmanaged during unrelated builtin edits, and seed replacement creation from the effective value.

Acceptance on macOS arm64 passed 47 focused resource/server/editor/detail tests, typecheck, lint/design-system, format, and backend/web builds. Focused coverage includes true/false/absence, invalid API types, effective override application and preservation, replacement materialization, dirty/conflict/save behavior, and accessible compatibility-only help. Two unrelated parallel-load timeouts in broader package attempts passed immediately in isolation; the prior full SUB-17 workspace run remains the current broad baseline. Real-Pi was not run because no runtime, prompt, process, or Pi launch path changed. Windows/Linux runtime, screenshots, and Electron E2E were not run. No preload/main, packaging, dependency, native storage, or sync seam changed.

### SUB-17 — default progress compatibility metadata

Custom and replacement agents now scan, edit, persist, clear, and display native’s `defaultProgress` boolean. Native’s audited/current runtime has no consumer for this field, so Electron accurately treats it as compatibility metadata rather than claiming it changes child reporting. Native-compatible persistence writes only `true`; false/absence resolves and displays as No. Effective builtin overrides apply true or false, remain unmanaged during unrelated builtin edits, and seed replacement creation from the effective value so a cleared builtin default cannot silently return.

Acceptance on macOS arm64 passed the full workspace suite (including 774 server, 270 web, and 168 resource tests plus 39 Rust tests), typecheck, lint/design-system, format, native addon build/smoke, and backend/web builds. Focused resource, override, route, editor, replacement, and detail tests cover true/false/absence, invalid API types, override preservation, replacement materialization, dirty/conflict/save behavior, and accessible explanatory copy. Real-Pi was not rerun because no runtime, prompt, process, or Pi launch path changed; the prior SUB-16 suite remains the current runtime baseline. Windows/Linux runtime, screenshots, and Electron E2E were not run. No preload/main, packaging, dependency, native storage, or sync seam changed.

### SUB-16 — default delegated outcome

Custom and replacement agents now scan, edit, clear, persist, and display native’s typed `defaultExpectedOutcome`: report only, edit files in worktree, write/update project file, or direct project writes. Named child launches and continuations re-resolve the current definition and add an explicit outcome contract without granting or revoking tools, MCP access, worktrees, or paths. Report-only is guidance; worktree editing is effective only when the caller supplied retained worktree isolation; write-project-file falls back to report-only without a validated per-run path; direct writes target the child’s actual cwd using only its existing configured tools. Missing or invalid values safely resolve to report-only. Existing builtin outcome overrides survive unrelated edits.

Acceptance on macOS arm64 passed all 94 pinned real-Pi tests (85 server and 9 pi-host), typecheck, lint/design-system, format, native addon build/smoke, and backend/web builds. The workspace suite passed every package and affected test; one unrelated known provider-failure timing test timed out in the broad concurrent run and passed immediately in focused isolation. Focused resource, editor/detail, override, server, named-child, and named parallel-worktree coverage proves round-trip/clear behavior, accessible help, ordered streaming, SUB-15 MCP preservation, truthful fallback contracts, configured-tool stability, and isolated worktree execution without parent-checkout mutation. Windows/Linux runtime, screenshots, and Electron E2E were not run; this shared Node/React/Pi slice changes no main/preload, packaging, native storage, or sync seam.

### SUB-15 / AGT-08 — native-compatible direct MCP adapter tools

Agent `tools:` frontmatter now recognizes native’s case-sensitive `mcp:<tool>` convention: scanning separates direct adapter names from ordinary Pi tools, while edits, custom files, builtin overrides, unknown fields, and `tools: false` retain the native combined persistence shape. The live editor and agent detail show the combined list with clear help that direct adapter names can be stale and neither connect nor grant access to Agent Deck-managed MCP servers. Named delegated children exclude `mcp:` entries from Pi’s `--tools` allowlist and receive ordered `MCP_DIRECT_TOOLS`; missing, empty, and anonymous policies overwrite inherited state with `__none__`. Continuations re-resolve the named agent through the same launch path. Agent Deck’s project MCP assignment trust and `mcpServers` remain separate.

Acceptance on macOS arm64 passed the full workspace suite (including 772 server, 264 web, and 163 resource tests plus 39 Rust tests), all 93 pinned real-Pi tests, typecheck, lint/design-system, format, native addon build/smoke, and backend/web builds. Focused coverage proves scanner/writer/override round-trip and clear semantics, native-compatible editor payload/error accessibility, ordered environment propagation, inherited-policy replacement, and exclusion from Pi launch arguments. Existing real-Pi named-agent and streaming suites prove resolved named configuration and multiple ordered child deltas before finalization; no external `pi-mcp-adapter` is bundled, so actual third-party adapter execution was not exercised. The implementation is shared Node/React logic with no platform branch; Windows/Linux runtime and screenshot/Electron E2E were not run. No preload, Electron-main, packaging, dependency, or new storage seam changed. AGT-06 remains active for SUB-16–SUB-21.

### SUB-13 — dormant direct-launch sheet is not shipped behavior

SUB-13 was removed as an incorrect behavioral gap. At both the fixed native baseline `6ba89a5` and current native `9efbe6c`, `PiNativeSubagentRunSheet` exists only as an uninstantiated SwiftUI definition, and the public selected-session `AppViewModel.runNativeSubagent` convenience has no UI caller. The composer’s shipped Deck-agent control only enables or disables parent delegation; native’s own orchestration prompt says delegation occurs through `managed_subagent` or `managed_parallel`. Pre-rewrite history confirms the former sheet presentation was explicitly removed before the audited mainline snapshot. Electron therefore matches the portable shipped outcome; adding a direct-launch UI would be new product work, not parity closure. This correction follows the existing SUB-10 and AGT-10 precedent that unreachable native definitions are not user behavior.

### SUB-06 — declared subagent reads

`managed_subagent` now accepts an optional bounded list of current project-relative paths for fresh and continuation turns. The bridge trims and stable-deduplicates paths, rejects the entire call before launch for empty, control/multiline, absolute POSIX/Windows/UNC/drive-relative, traversal, count, or UTF-8 byte-limit violations, and sends only read-first hints—never preloaded file contents. Each turn retains its validated list in app-owned `input.md` evidence, while the latest list survives restart in additive v4 run records; legacy v1–v3 records remain readable. The shared UTF-8 budget proves a maximum 50,000-byte task plus accepted reads fits the native 50 KiB artifact limit exactly.

Acceptance on macOS arm64 passed 60 focused server tests, the full 84-test server and 9-test pi-host pinned real-Pi suites, typecheck, lint/design-system, format, native addon build/smoke, and backend build. The real-Pi continuation scenario proves fresh and continuation hints, same-child history isolation, stable run/card identity, per-turn durable evidence, and multiple ordered live deltas before finalization. The broad workspace suite passed every package except one unrelated existing `sessionManager.test.ts` provider-failure timing timeout, which passed immediately in focused isolation; the same failure reproduced before the final implementation correction. Independent re-review found no material issue after the artifact-budget fix. Validation uses both POSIX and Windows path semantics in shared Node code; Windows/Linux runtime execution was not repeated. No renderer, preload, Electron-main, packaging, or user-facing visual surface changed. `docs/sync-seams.md` records declared paths as privacy-sensitive device-local subagent evidence.

### SUB-11 — caller-bounded managed parallel concurrency

`managed_parallel` accepts optional integer `concurrency` from 1 through 8, defaults to 4, and caps the worker count at the task count. A bounded work-conserving pool starts each durable run, artifact root, and optional SUB-12 worktree only when its slot begins; settled output remains in input order with the existing mixed-failure semantics. Parent teardown rejects still-queued tasks in place without entering `runSubagent`.

Focused server coverage proves schema rejection before launch, default/requested/capped limits, deferred overlap, reverse completion with failure, ordered rendering, worktree forwarding, and teardown suppression. The pinned real-Pi scenario uses controlled provider gates for three worktree-isolated tasks at concurrency two, proving actual overlap never exceeds two, ordered incremental deltas/results, three distinct retained worktrees, and an unchanged parent checkout. Local acceptance recorded 651 passing server tests (3 skipped), all 75 pinned real-Pi tests, typecheck, lint/design-system, and format checks. Native addon rebuild was blocked because Cargo was unavailable; native code was unchanged and the existing macOS arm64 addon passed smoke before the real-Pi suite. This call-local runtime scheduling adds no persistence or sync seam and no renderer/Electron surface.

### SUB-12 — isolated worktrees for managed parallel children

`managed_parallel` now accepts top-level optional `worktree`; false/omitted preserves the parent cwd, while true requires a distinct app-owned detached Git worktree at the parent's proven repository/HEAD for every fresh child. Allocation and exact Git/repository proof complete before that child's Pi starts, with no checkout fallback. Durable v3 child records retain opaque native identity, parent-repository identity, immutable allocation-base commit, and internal path evidence across completion, failure, stop, and restart. Parent deletion stops children first, then re-proves native identity and exact detached Git registration, removes and prunes worktrees, and only then removes artifacts/records; unsafe cleanup remains retryable.

Local acceptance passed the native addon build/smoke, typecheck, lint/design-system/format checks, the full workspace suite (including 615 server tests), all 74 pinned real-Pi tests, and the web/backend builds. The real-Pi scenario proved two writer children used different retained worktrees, the parent checkout remained unchanged, multiple ordered deltas arrived for each child before finalization, restart preserved ownership evidence, and parent deletion removed both registrations/worktrees and durable records. Full E2E recorded 217 passed/14 skipped; three unrelated existing failures remained in Loop retry focus, installed Pi 0.83.0 versus the test's 0.82.0 expectation, and the terminal shortcut. The full bundled-runtime build was blocked by unavailable private-package credentials after native, web, and backend builds passed. Runtime acceptance was macOS arm64; portable path normalization, spaces, commit-then-delete, post-add crash recovery, allocation/deletion serialization, and parent-path replacement rejection were covered locally, while Windows junction/reparse and Linux runtime execution remain unrun in this slice. `SessionWorktreeStore` supplies held native identity for every app-owned child leaf, but its managed-root boundary cannot safely capture an arbitrary ambient parent repository. Parent replacement is therefore bounded by repeated canonical Git repository/commit proof immediately before `git worktree add`, then exact registration/repository/native-identity proof immediately before Pi spawn and deletion; a same-process ambient-parent swap remains a bounded filesystem race rather than being papered over with an unsafe generic inode token.

### SES-09 — durable provider auto-retry transcript status

Pinned Pi retry events now update one stable, accessible transcript card per retry burst through `retrying`, `succeeded`, or `gave_up`, preserving ordered incremental delivery rather than buffering. Provider-error assistant rows collapse only when their actual role and error stop reason prove the pairing; positive message ordinals reconstruct that pairing across reconnect, restart, truncation, and process-exit finalization without hiding shifted history. Bounded durable records are sanitized and revalidated, stale live retries become interrupted failures, history-rewriting forks/rebinds clear unsafe ordinal anchors, and full-session duplicates retain valid ancestry. Recoverable retries do not override SES-11/12 terminal-failure precedence, while final failure, cancellation, recovery, and shutdown retain their existing semantics. The renderer exposes retry-only polite live status, static terminal cards, quota/reset details only when supplied, keyboard-scrollable bounded diagnostics, and local date/year formatting without forced focus.

Retry evidence uses the existing device-local session metadata persistence callback and `SessionIndex` boundary; `docs/sync-seams.md` now records that supplemental seam and its bounded/non-canonical role. Accepted macOS arm64 validation passed the pinned Rust 1.88 native-addon build/smoke, typecheck, lint/design-system, format, and the full workspace suite: **1,484 Vitest tests passed/6 skipped plus 39 Rust tests passed (1,523 passed total)**. All pinned real-Pi suites passed (**87/87: 79 server and 8 pi-host**), including deterministic live retry, durable failure, restart hydration, duplicate collapse, and later recovery. Focused retry Playwright passed **1/1**; broad Playwright recorded **227 passed/14 skipped/2 failed**, with only the unrelated installed Pi 0.84.1 versus expected 0.82.0 assertion and independently reproduced terminal-shortcut failure. Light and dark narrow screenshots were inspected locally for wrapping, hierarchy, contrast, and overflow; artifacts remained ignored. Native, web, and backend builds passed, while the aggregate bundled-runtime build stopped at `build:pi-runtime` because current credentials could not fetch private `@a-streetcoder/skill-engine-native` (registry 404). Runtime validation was macOS arm64 only; Windows, Linux, macOS x64, and packaged execution were not run. **E:** retry protocol/ingest/reducer, session manager/index persistence, `cells.tsx`, focused domain/server/web/real-Pi/Playwright tests. **N:** `PiAgentRunnerService.swift`, `PiAgentRetryCardView.swift`, transcript native blocks.

### SES-11 / SES-12 — durable terminal failure outcome (inseparable closure)

These rows close together because a durable failed state without a safe explanation is incomplete, while an error string without an authoritative terminal state has no reliable lifecycle.

**SES-11 acceptance — durable failed state.** `SessionMeta.status` persists only terminal `failed` through the existing session index and survives restart. Provider/runtime failure events and unexpected process exits are authoritative; queued provider detail drains before generic exit finalization, so a later nonzero exit cannot replace the specific cause. Recoverable tool errors, RPC timeouts, user stop, delete, rebind teardown, shutdown, and app quit are excluded. Exactly-once exit/meta cleanup remains intact, including asynchronous ingestion settlement during stop. Failure clears only at authoritative recovery boundaries: an accepted prompt/`agent_start`, successful resume or rebind, and fork materialization. **E:** `packages/contracts/src/protocol.ts`, `SessionManager.ts`, `services/sessionManager.ts`, `PiProcess.ts`, persistence and focused server/real-Pi tests.

**SES-12 acceptance — durable safe latest error and accessible presentation.** `SessionMeta.lastError` persists with the failed state, strips ANSI/control characters, collapses whitespace, redacts bearer/API-key/token/secret forms including quoted config values, and caps output at 2,048 Unicode code points; process stderr and private project content are not persisted wholesale. Session rows expose a non-dimmed failure icon/subtitle, failed status, and descriptive accessible label, while the transcript retains a polite live status with the same bounded detail. Restart preserves both row and transcript evidence, and the same authoritative recovery boundaries clear both fields. **E:** `sessionFailure.ts`, `SessionsPanel.tsx`, `Transcript.tsx`, status selection helpers, focused unit/component/E2E tests, and `docs/sync-seams.md`.

Acceptance passed the full workspace suite, all 76 server pinned real-Pi tests, the focused restart/recovery E2E scenario (1/1), typecheck, lint, design-system and format checks, native addon build/smoke, repeated provider-error precedence stress, and deterministic asynchronous teardown regression coverage. Independent review found no material findings. CI [run 30771433278](https://github.com/a-streetcoder/agent-deck-electron/actions/runs/30771433278) subsequently passed the unit matrix on Linux, Windows, and macOS, Linux/macOS real-Pi suites, and Linux/Windows E2E. On Windows, the real-Pi job specifically passed `session-failure.pi.test.ts` and the shutdown-exclusion regression `ask-user-shutdown.pi.test.ts`; the overall job then failed only on the unrelated `compact.pi.test.ts` outer 60-second timeout, now raised to 180 seconds and passing locally, so the run itself is not recorded as green.

### SES-13 / SES-28 / SES-15 — durable all-session attention (inseparable closure)

These rows close together because durable per-session attention is not useful unless every session is observed and shell badges count the same authoritative pending set.

The backend now owns `SessionMeta.needsAttention` in the device-local session index. It raises attention only for a successfully completed user turn or a human-blocking ask-user, supervisor-question, or extension UI request; provider retry, cancellation, abort, teardown, process exit, and startup/resume idle do not raise it. Retry state preserves the pending user turn. A strict acknowledge-only route accepts literal `false`, serializes with deletion, and never lets acknowledgement resurrect a removed session. Legacy or malformed persisted values normalize safely, hydration establishes a silent baseline, and `docs/sync-seams.md` records this supplemental device-local state rather than claiming cross-device canonical sync.

The renderer observes metadata for all sessions, reconciles HTTP catalogs against newer WebSocket generations, and acknowledges only after the selected chat is actually visible and the document is focused. Selection while hidden does not clear attention. Notification routing is token-scoped so activating pending session B cannot accidentally acknowledge previously selected pending session A; stale catalogs and delayed acknowledgements cannot overwrite newer attention transitions. Electron main independently refreshes the authoritative catalog, deduplicates notifications by durable session and attention epoch, and derives a distinct-session count without a focus-wide clear. macOS and Linux use the platform badge count; Windows uses a bounded generated 32×32 raster taskbar overlay, including `99+`, rather than claiming unsupported numeric `app.setBadgeCount` behavior.

Session rows expose visible and semantic attention state, per-project/all-project counts, tooltips, and accessible labels. Failure remains visually and semantically primary when both states exist. A polite app-level live region announces new attention edges, including repeated false→true epochs and simultaneous-session summaries.

Accepted macOS arm64 validation passed native addon build/smoke, typecheck, lint/design-system/format, and the full workspace gate (**1,540 passed/6 skipped**, including 39 Rust tests). All pinned real-Pi tests passed (**88/88**). Full Playwright recorded **228 passed/14 skipped/3 failed**: the unrelated installed-runtime Doctor version mismatch and terminal shortcut failure reproduced focused, while the unrelated MCP assignment timeout passed focused. Attention browser and real-Electron scenarios passed, including restart hydration, acknowledgement, distinct hidden-session badges, notification routing, Windows raster decoding, and clean shutdown. Native, web, and backend builds passed; the aggregate bundled-runtime build stopped at `build:pi-runtime` because current credentials could not fetch private `@a-streetcoder/skill-engine-native` (registry 404), so package validation was not completed. Runtime validation was macOS arm64 only; Windows, Linux, macOS x64, and packaged execution were not run. **E:** session contracts/persistence/manager/routes, desktop main/preload and raster overlay helper, renderer attention/notification routing/session rows, focused unit/real-Pi/browser/Electron tests, `docs/sync-seams.md`, and `docs/effect-migration-plan.md`. **N:** `PiAgentSessionModels.swift`, `PiAgentSessionStore.swift`, native notification routing and badge behavior.

### MCP-03 — automatic OAuth callback

MCP sign-in now starts a hardened, one-shot IPv4 loopback listener before authorization, opens the system browser, validates the exact OAuth state and PKCE generation, exchanges the code, refreshes only the owning project/server, and updates the accessible MCP status without requiring pasted input. The stable configured redirect preserves dynamic-client and refresh-token continuity across restarts; bind failure and external redirects retain an explicit manual code/full-URL fallback. Manual and automatic completion share an exactly-once claim, while supersession, unassignment, removal, URL changes, project hiding, and shutdown cancel and drain owned work. Foreign-state callbacks cannot consume another app instance's listener.

OAuth records are bound to the exact normalized server URL before bearer credentials can reach the MCP client. Versioned collision-safe keys replace the ambiguous legacy delimiter key; unproven legacy credentials are deleted rather than migrated. Removed or recreated definitions cannot inherit old tokens. The device-local credential boundary and no-sync policy are recorded in `docs/sync-seams.md`.

Accepted on macOS arm64. The native addon build/smoke, workspace typecheck, lint, format, design-system check, focused MCP/package/server/renderer suites, real HTTP loopback/route integration, and all six focused MCP Playwright scenarios passed. The full workspace suite passed 1,014 server tests plus all other packages before one unrelated session-attention timing test failed; that exact test passed immediately in isolation. The pinned real-Pi MCP suite passed, while broader real-Pi runs suffered unrelated host stalls/timeouts. Full Playwright passed the MCP coverage but later stalled on unrelated history-action coverage after an unrelated extension test failure. Windows/Linux runtime and a live third-party OAuth provider were not exercised; shared Node HTTP and browser-bridge paths have no platform-specific branch. Independent review found no remaining material MCP-03 issue.

### MCP-01 — remote HTTP MCP add form

The MCP Add server form now offers Local (stdio) or Remote (HTTP). HTTP posts `{ name, url }` through the existing `POST /mcp` writer into global `~/.pi/agent/mcp.json`. The new row appears with an `http` badge, stays disconnected and unassigned, and can be removed. Invalid `http(s)` URLs and duplicate names disable Add with an explanation. The form has visible Name/Command/URL labels, Enter submits from any field, and Escape restores focus to Add server. Headers, OAuth client fields, cwd, paste/import, SSE, and in-app edit remain separate rows.

Accepted local macOS arm64 validation passed focused MCP renderer tests (17/17), workspace typecheck, lint/design-system, format, and focused Playwright `mcp.spec.ts` (5/5), including the HTTP add/remove scenario. Independent correctness review accepted the add-only slice. UX review findings for labels, validation copy, Enter, Escape focus, and selected-segment contrast were fixed before closure. Windows/Linux runtime, packaged Electron, and `pnpm test:pi` were not rerun; no backend, persistence, preload, Electron-main, packaging, or sync seam changed. **MCP-01 closed. E:** `McpScreen.tsx`, focused renderer/E2E tests. **N:** `MCPServersScreen.swift` remote add fields.

### MCP-02 — edit an existing global MCP server

Editable global MCP rows now expose an in-app editor with a locked identity and separate Local (stdio) Command/Arguments or Remote (HTTP) URL fields. `PATCH /mcp/:id` accepts only usable app-owned global catalog entries, refuses environment-shadowed and unlisted definitions, and persists through the existing `~/.pi/agent/mcp.json` writer. Reads expose only the editable definition fields; env, cwd, headers, OAuth material, project definitions, and environment definitions remain undisclosed and uneditable. Writes preserve sibling servers, top-level unknown keys, and same-transport fields not owned by this slice while removing fields from the opposite transport. Quoted, empty, spaced, and control-character arguments round-trip without splitting the command itself.

Accepted local macOS arm64 validation passed focused renderer (24/24), server route (8/8), and resource writer (15/15) tests; workspace typecheck; lint/design-system; format; and focused Playwright `mcp.spec.ts` (6/6), including HTTP edit persistence. Save locking, project-switch load/focus fencing, fail-closed unusable-key handling, accessible edit/close state, and structured argument round-tripping were corrected after independent correctness and UX reviews; both reviews accepted the final slice. Windows/Linux runtime, packaged Electron, the full workspace test suite, and `pnpm test:pi` were not run; no preload, Electron-main, native module, packaging, dependency, or sync seam changed. **MCP-02 closed. E:** merge-preserving MCP resources writer, MCP GET/PATCH routes, `McpScreen.tsx`, focused resource/server/renderer/E2E tests. **N:** `MCPServersScreen.swift`, `MCPConfigWriter.swift`.

### MCP-04 — master MCP pause

A persisted global switch now pauses model/runtime MCP without changing server definitions, default/project/agent assignments, OAuth credentials, or config bytes. Missing and malformed legacy values deliberately default **on** to preserve established Electron behavior; explicit pause survives restart. The authoritative injectable `McpPolicyStore` persists before runtime mutation. Pausing immediately denies admission, publication, dispatch, and reconnect; aborts pending discovery; removes tools/ownership; attempts every client teardown; and surfaces cleanup warnings without rolling back durable truth. Enabling reconnects only current configured trusted grants. OAuth and catalog management remain available while paused, while hidden projects cannot regain OAuth access.

The policy participates in ordinary, named, and no-project RES-12 fingerprints. Active turns defer replacement to a safe idle boundary, idle sessions rebind, parked sessions adopt current launch policy, and new named delegated children receive no direct-MCP policy while paused. Already-running third-party direct-adapter children retain their launch-time environment because Agent Deck does not own that external adapter's revocation protocol; app-managed MCP dispatch is denied immediately. The accessible MCP switch has truthful loading, On, Paused, Saving, cleanup-warning, and persistence-error states; preserves focus; avoids broadcast races and header jitter; keeps management controls usable; and presents paused rows plus an explained disabled reconnect action.

Local macOS arm64 acceptance passed workspace typecheck, lint, format, design-system, native addon build/smoke, independent final review with 1,045 server tests and 444 renderer tests, and the complete pinned real-Pi suite (**117/117**). Real-Pi coverage proves ordinary/named/no-project on→off→on replacement at safe boundaries, actual tool removal/restoration, and multiple ordered deltas before finalization. Full Playwright recorded **247 passed/14 skipped/2 failed**; the failures were the unrelated installed-Pi-version expectation and existing terminal shortcut. Direct light/dark inspection covered normal and 480px layouts, On/Paused/Saving/error, long names, focus, contrast, and fixed switch geometry; policy error contrast measured above 15:1 in both themes. The pre-existing 480px app-shell minimum width still requires horizontal scrolling and can heavily truncate server-row identity/status. Windows/Linux process teardown, packaged Electron, and real Electron-window visuals were not run. **MCP-04 closed. E:** `McpPolicyStore`, settings persistence, MCP routes/manager, server/launch/session refresh gates, `McpScreen.tsx`, `sync-seams.md`, and focused unit/real-Pi/Playwright tests. **N:** `AppSettings.mcpEnabled`, MCP settings views and runtime reconciliation.

### MCP-08 — effective config source provenance

Every supported effective MCP definition now carries the exact winning origin from the same coherent config snapshot used to render its definition. Global and project entries identify their precise `mcp.json` path; environment overrides identify `AGENT_DECK_MCP_SERVERS` without exposing its value. Existing global editability and project/environment read-only ownership remain unchanged. The renderer shows concise ownership plus accessible full provenance, with safe truncation for long paths. Shadowed definitions and secret-bearing environment/config values are not returned.

This is additive read-only metadata: runtime resolution, persistence, OAuth, process lifecycle, Electron main/preload, packaging, and sync seams are unchanged. Local macOS/Chromium acceptance passed workspace typecheck, lint/design-system, format, all 229 resource tests, focused server provenance tests (10/10), renderer MCP tests (41/41), and focused MCP Playwright (8/8). Light/dark normal and narrow screenshots were directly inspected; long paths retained full title/accessibility text without control loss. Independent review found and then accepted the coherent-snapshot correction that prevents provenance from racing a second config read. Windows/Linux runtime was not run; the implementation uses shared Node path/React logic and existing cross-platform path derivation. **MCP-08 closed. E:** resource MCP catalog source metadata, coherent effective snapshot, MCP route DTO, `McpScreen.tsx`, and focused resource/server/renderer/Playwright tests. **N:** winning `MCPServerEntry.sourcePath` and MCP source presentation.

### MCP-05 — default MCP assignment

A durable **All Projects** assignment now grants configured MCP servers to ordinary project chats in union with each project's explicit assignments. Named-agent chats remain narrowed to the agent's configured declarations, and no-project chats receive no default grant. Stale names stay visible and removable but inert. Assignment writes and reads use one injectable `McpAssignmentStore`; failed filesystem writes cannot change live authorization. Unassignment cleans credentials only where no effective project or named-agent grant remains, definition deletion clears default and project references, and assignment changes participate in RES-12 controlled session replacement.

The MCP screen exposes separate accessible All Projects and project controls, distinguishes inherited state with or without a preserved explicit assignment, fences project-switch races, reports load/save failures honestly, and restores focus after successful refresh. Local macOS arm64 acceptance passed workspace typecheck, lint, format, design-system, native addon build/smoke, 1,027 server tests and 440 renderer tests in independent final review, the focused assignment Playwright suite (7/7), and all affected pinned real-Pi MCP scenarios (7/7), including live default removal, unchanged named/no-project scopes, actual tool exposure, restart persistence, and multiple ordered deltas before finalization. The broad real-Pi run passed 115 tests and retained one unrelated pre-existing OAuth callback expectation mismatch; full Playwright passed 244 tests with four unrelated existing Loop, onboarding, installed-Pi-version, and terminal-shortcut failures. Direct light/dark inspection at 1280×820 and 760×760 covered long names, inherited variants, busy rollback, error, focus, and overflow; narrow server identity/status density remains a bounded responsive follow-up. Windows/Linux runtime and packaged Electron were not run; this shared TypeScript/JSON/React path changes no Electron main/preload, native module, or packaging surface. Agent frontmatter references are not rewritten on definition deletion because authored-agent mutations still lack an authorized store seam; absent definitions keep those names inert. **MCP-05 closed. E:** `McpAssignmentStore`, settings/project persistence, MCP/project/session routes, launch-resource resolution, `McpScreen.tsx`, `sync-seams.md`, and focused server/renderer/real-Pi/Playwright tests. **N:** `AppSettings.defaultMcpServerNames`, `MCPAssignmentTests.swift`, MCP assignment views.

### CMD-03 — composer slash catalog browser

In a project session, `/` at the start of the composer now opens a Commands / Prompts / Skills / Loops browser over existing catalogs rather than Pi `get_commands`. Empty categories are omitted. Typing searches the snapshot in memory. Enabled injected commands, catalog prompts and skills (including unassigned), and available loops appear; disabled builtins/skills and disabled injected commands are omitted. Prompt accept seeds the editor body. Skills accumulate as chips; one active skill sends `/skill:name`, while inactive or multi-select skills inline bodies. Commands leave the composer ready for arguments. Loop accept opens the existing Loop Bank create or launch UI and does not send a message. No-project / general-chat sessions keep an empty universe. Native named skill-collection groups are not recreated; engine collections already flatten to ordinary skills. Optional `titleSource` is first-turn-locked and not persisted. `GET /sessions/:id/commands` remains for Pi verification.

Accepted local macOS arm64 validation passed workspace typecheck, lint/design-system, format, focused domain/server/renderer suites, and focused Playwright `slash-universe` plus `composer-suggest` (11/11). The first-turn title lock, empty-search send suppression, no-project hide, Escape category-back, and listbox `aria-activedescendant` wiring were corrected after independent review. A concurrent full `pnpm test` hit an unrelated existing `git.test.ts` 5-second timeout that passed immediately in isolation. Windows/Linux runtime, packaged Electron, and `pnpm test:pi` were not rerun; no preload, Electron-main, packaging, injected-command store, or sync seam changed. **CMD-03 closed. E:** `slashUniverse` contracts/domain/server assemble, session route, composer suggestions/chips, Loop Bank handoff, focused unit/Playwright tests. **N:** `SlashUniverse.swift`.

### PRM-08 — prompt catalog search

The prompt catalog now has controlled live client-side search over every available native-equivalent field: prompt name, slash invocation, description, scope, file path, and body. Matching is trimmed and case-insensitive; clearing restores the original catalog objects and actions, a loaded catalog with no matches has a dedicated accessible status, and the true empty state cannot flash while loading. The existing design-system text field supplies keyboard input, an accessible name, and a focus-preserving clear action; no backend, persistence, IPC, or dependency surface changed.

Acceptance passed `pnpm typecheck`, lint, format, design-system, the full workspace suite (including 220 renderer tests), all pinned real-Pi suites (8 pi-host and 77 server tests), the production web build, and focused Prompt Playwright coverage (5/5). Full E2E recorded 222 passed/14 skipped with two unrelated existing failures: Doctor observed installed Pi 0.83.0 while the test expects pinned 0.82.0, and the terminal keyboard shortcut did not open the drawer (also failed focused rerun). Focused renderer coverage proves each search field, case/trim, clear/focus restoration, no-results versus empty/loading states, and original-object action behavior. macOS Chromium visual inspection covered the representative catalog at 1280×720. Windows/Linux runtime execution was not performed; this renderer-only path has no platform-specific branch.

## Corrections to older parity claims

- Provider login is not OAuth-only. API-key and OAuth login, search, grouping, method choice, browser launch, re-authentication, and credential replacement are present. Only an explicit renderer logout/remove action is missing.
- macOS packaging is not parked. Electron has an electron-builder DMG path with hardened runtime, signing, notarization, and bundled pinned Pi.
- Durable backend-owned attention, all-session observation, distinct-session badges, and safe notification click routing are present. SES-14 remains open for persisted notification timestamps/state and its additional audit semantics.
- Basic named, caller-bounded parallel, streaming, same-child continuation, optional detached per-child parallel worktrees, app-owned child artifacts, and parent-owned reopenable/inspectable child transcripts work. Generic children remain deliberately subordinate to their parent rather than appearing as top-level sidebar sessions. The missing area is the richer parent tool catalog and configuration depth.
- **AGT-10 was not a valid shipped-native parity gap at native `9efbe6c` and is removed.** Native defines `makeDuplicateAgentDraft`, but it has no caller in `ContentView`, `AgentManagementViews`, `AgentDeckCommands`, or tests, so the dormant helper is not reachable UI. Electron also has no general duplicate action; this correction makes no Electron feature or runtime claim.
- **MEM-16 was not a valid shipped-native parity gap and is removed (2026-08-17 correction).** Native declares `agentMemoryRetentionDays: Int = 120` (`AppSettings.swift:176`), and the synthesized `Codable`/`Hashable` conformances encode, decode (`:296`, clamped to at least 1) and persist it — but no BEHAVIOR consumes it: there is no setter, no Settings control, no age-based status transition, no launch sweep, and no deletion path. The only age cutoff in the app is unrelated (`AppViewModel.swift:516 cleanupOrphanedNativeSubagentArtifacts(retentionDays: 30)`, which deletes orphaned subagent artifacts, not memories), and the only `cutoff` in `AgentMemoryStore.swift:362` is a relevance SCORE ratio for ranking. So there is no retention behavior for the port to match: building age-based memory deletion would exceed the reference app AND be destructive, silently removing a user's project knowledge under a policy native never applies. Cross-model check: Codex assessed this closure and returned one LOW — that "nothing reads it" needed the precision above (the field IS persisted; only its behavior is absent) — and otherwise supported closing rather than implementing. Electron keeps its own memory controls (pause, subagent memory, transcript cards, injection character budget); this correction makes no new Electron feature claim.

- **SUB-10 was not a valid single-child parity gap at native `13083be` and is removed (2026-07-31 correction).** Native `ManagedSubagentParams` and `PiManagedSubagentBridgeRequest` omit a worktree field, while `runManagedNativeSubagent` hard-codes `useWorktreeIsolation = false`. `PiNativeSubagentRunSheet` declares a worktree toggle but has no call site, so latent `PiSubagentWorktreeService` / `runSingle` infrastructure is not shipped user behavior. Loop's **New worktree** target is a separate Loop launch/review workflow, not single `managed_subagent` parity. SUB-12 is now closed: top-level `managed_parallel.worktree` allocates a distinct retained app-owned detached worktree per fresh child and fails closed before child Pi spawn.
- MCP HTTP creation, global editing, automatic OAuth callback, global runtime pause, live tools, All Projects defaults, explicit project assignments, project-config resolution, effective source provenance, and agent allowlists work. Broader discovery and advanced config remain incomplete.
- Packaged Electron normally needs no manual Pi installation. Doctor install/update/repair gaps mainly affect development, external runtimes, and independent runtime maintenance.
- Whole-session duplicate remains a separate convenience action; stable-entry Pi fork/edit/rerun is also present.
- **SES-24 is removed by user product-scope decision for Electron.** The portable product intentionally retains ordinary stable-entry Fork plus explicit named-agent new-chat launch, but does not add a combined fork-as-agent handoff. This is not an implementation-parity claim.
- **GIT-08 was not a valid ordinary-session parity gap and is removed (2026-08-02 correction).** Native has no scheduled, startup, age-based, or orphan cleanup for ordinary session worktrees or branches. It cleans them on explicit owning-session deletion and optionally after merge when retention is disabled. Native's 30-day startup cleanup is limited to unreferenced subagent artifact directories. Electron matches the portable session lifecycle and now safely removes the exact owned branch on explicit deletion under GIT-06; an automatic prefix- or age-based scavenger would add unsupported destructive behavior rather than parity. This is a native-source parity correction, not a feature-runtime validation claim; CI [run 30771433278](https://github.com/a-streetcoder/agent-deck-electron/actions/runs/30771433278) supplies the cross-platform runtime evidence stated for the underlying GIT-06 deletion path, not a separate GIT-08 feature test.
- Loop worktree isolation and retained-worktree review retain the focused-runtime validation recorded for LOOP-27. Apply/Discard was implemented later and is covered by focused server/E2E tests; it is not assigned that same certification here. General Git review remains read-only, while the dedicated Loop path applies only the exact reviewed patch after source-checkout preflights or implements Discard by atomically archiving the proven worktree under Agent Deck's private root while retaining the branch and artifacts.
- **Project-local authored skills, agents, and prompts ARE a native feature — the "intentionally unsupported on both sides / not a native requirement" row under _Gaps closed or aligned_ is incorrect (2026-07-27 correction).** Native `PiScanner.swift` scans `<project>/.pi/{skills,agents,prompts}` at scope `.project` and `<project>/.agents` at scope `.legacyProject` (PiScanner.swift:28–66, 406); `AppRefreshService.swift` watches those dirs per project. The electron refactor dropped them to global-only and encoded it in resource tests — a **regression**, not alignment. **Display parity is restored** for `<project>/.pi/{skills,agents,prompts}` (scan + watch, project shadows global, scope `project`). **Project skill writes are also restored:** P3 is done, so project skill create/edit/delete/rename/local-import mutations go through `EngineSkillStore`, and project skills materialized at `<project>/.agents/skills` are scanned back into the app. Project agent/prompt writes, native `<project>/.agents` legacy-project agent discovery, and project agent assignments/defaults remain open.
- **Shared skill-engine status is P3 and P4 done.** The pinned 0.1.5 engine owns in-app skill create/edit/delete/rename/local-import writes, including project scope, plus Git collection import/sync, per-file conflict resolution, staleness checking, and recovery behind `EngineSkillStore`; the dead `NativeSkillStore` and duplicate collection path are gone. SKL-15/16 are closed. Pre-0.1.5/base-less imports remain a bounded upgrade caveat: re-import establishes a base snapshot, while the compatibility UI can still offer whole-skill resolution.
- **Instruction-file watching is a shared limitation, not an Electron parity gap (2026-07-29 correction).** Neither native `AppRefreshService.watchedURLs` nor Electron `watchDirs` includes instruction candidates, and both mounted editors retain their current snapshot until the view or scope reloads. Fresh sessions read current instruction files from disk. RES-12 subsequently closed running-session semantic replacement when an explicit resource-change signal occurs, without expanding this watch boundary.
- **MCP configuration is explicitly reloaded, not automatically watched, on both sides (2026-07-29 correction).** Native rereads MCP files when the user opens or refreshes its MCP screen; it does not include those files in `AppRefreshService.watchedURLs`. Electron now exposes **Reload config** on its MCP screen: it rereads the supported global and selected-project `.pi/mcp.json`, preserves project-over-global and environment-override precedence, retains unchanged project-scoped live clients, and reconciles added, changed, and removed assigned servers in serialized snapshot order. Malformed JSON fails closed and preserves the affected project's current connections. Effective source provenance is now present; broader foreign paths remain MCP-11. Refreshing launch-time non-MCP resources inside already-running sessions remains RES-12.
- **Broad existing-ancestor watch roots are not an Electron parity gap (2026-07-29 correction).** Native can use the full home directory as its recursive FSEvents root when `~/.agents` is absent. When both a deeper target and its immediate parent are absent, native omits that event root and relies on its 300-second fingerprint fallback. Electron instead climbs to a contained existing ancestor, filters events against canonical resource targets, and detects later catalog creation promptly without creating directories. After the focused RES-04/05 containment and linked-loop validation, exact-target polling would be optional hardening rather than required portable parity.
- **External Loop-file watching is a shared limitation, not an Electron parity gap (2026-07-29 correction).** Neither native `AppRefreshService.watchedURLs` nor Electron `watchDirs` includes `~/.pi/agent/loops`. Both reload the catalog on initial view load and after their own Loop CRUD, and both reread the persisted definition before a normal launch. An external edit can therefore leave an already-mounted Loop catalog stale on either side until another reload; live Loop-file watching remains optional product hardening.
- **Extension files are explicitly refreshed, not automatically watched, on both sides (2026-07-29 correction).** Native omits extension catalogs from `AppRefreshService.watchedURLs` and exposes a Refresh action on its Extensions screen. Electron retains the same no-watch boundary and now exposes **Refresh** on its Extensions screen; it rereads the selected project's global/project catalog and content-derived conflict metadata without navigating away or restarting. Running sessions retain their launch-time extension set under RES-12.

### SES-21/22/23 — stable-entry fork, edit, and rerun

Provider-backed user turns now gain their stable Pi entry identity from canonical active ancestry and expose accessible Fork and Re-run actions. Fork uses pinned Pi `get_fork_messages`/`fork(entryId)`, publishes an independent Deck row only after launch and history seeding, keeps source worktree deletion ownership solely on the source, and persists a non-owner dependency that prevents source delete/merge cleanup while a target still uses that checkout. It seeds the target-owned editable draft with app-owned images, pastes, files, and folders. Re-run stages original durable delivery, forks and prompts the already-rebound Pi exactly once, waits for idle, then deferred-publishes the rebuilt same Deck identity. A typed server rebind push makes every connected same-id subscriber discard its old sequence ancestry and request the replacement transcript snapshot; the durably persisted stream generation also forces disconnected clients with old or missing generations to snapshot instead of replaying a colliding sequence from the replacement bus. History, deletion, and merge share one per-session claim. Nested forks retain the original worktree owner's non-owner dependency, ambiguous fork failures compare Pi's pre/post session handle, failed transactions restore the indexed source and roll back staged sidecar ownership, and abandoned Pi files are retained rather than deleted unsafely.

Acceptance on macOS arm64 includes the preceding full 636-test server suite (3 skipped), 203-test renderer suite, and 75-test pinned real-Pi suite; the final merge-claim, nested-dependency, and durable-generation revision subsequently passed workspace typecheck plus focused server, renderer/transport, real-Pi, and Playwright checks. Focused evidence covers duplicate text with exact entry identity; ordered image bytes/MIME, paste, file, and folder projection across sidecar/index re-instantiation; ended named-agent launch-plan retention; source/target worktree deletion and merge-cleanup boundaries; fault restoration/publication; exact-once rerun prompting; and two simultaneous same-id subscribers receiving ordered post-rerun deltas. Focused Playwright covers target draft focus/edit, same-row rerun, destructive confirmation, Escape/focus restoration, source resumability, exact source/target handles, and DELETE/action serialization. Full E2E recorded 218 passed/14 skipped and five failures; the two affected file/folder reload failures were fixed and passed together on a focused rerun, while unrelated existing failures remained in diff-comment hover stability, installed Pi 0.83.0 versus the test's 0.82.0 expectation, and the terminal shortcut. Typecheck, focused lint/format, and design-system checks passed. Fork's server-returned draft follows existing same-renderer session ownership and reload behavior; persistence of an unsent fork draft beyond renderer lifetime remains SES-35 and is not claimed here. A raw early-disconnect DELETE race was not injected; handler-scoped `try/finally` ownership and deterministic overlapping DELETE/history coverage were exercised. The shared Node/React/Pi path has no new platform branch; Windows/Linux runtime execution was not repeated.

### SES-25 — durable fork provenance

Stable-entry forks now publish versioned provenance atomically with the materialized target: the immediate source session identity, captured source title, selected stable Pi entry identity, and a deterministic bounded ancestry recap. The recap comes only from canonical active Pi ancestry before the selected origin, retains useful recent user, assistant, and thinking plain text, marks truncation, and excludes tool/status/custom noise plus attachment bytes and payloads. Fork-of-fork records only its immediate source; source rename does not rewrite the captured title; restart and source deletion retain the target evidence, while whole-session duplicate and rerun keep their existing semantics.

A transcript-top provenance card reopens the source through the existing session switch while it exists and reports honestly when it does not. Its recap dialog has a labelled heading, scrolling, explicit empty/truncated/long states, close control, focus trap, Escape handling, and focus restoration; malformed or unsupported future metadata is ignored safely. Accepted macOS arm64 validation passed the full workspace suite (including 663 server, 207 web, and 81 contracts tests), all pinned real-Pi suites (75 server and 8 pi-host), focused history-actions Playwright (3/3), static/design/format/typecheck, and the production web build. The shared Node/React path has no OS-specific branch. CI [run 30771433278](https://github.com/a-streetcoder/agent-deck-electron/actions/runs/30771433278) subsequently passed the portable unit suites on Linux, Windows, and macOS, full Playwright E2E on Linux, and browser E2E plus the Electron launch smoke on Windows, including the existing provenance unit/component and history-actions browser coverage. The overall run is not recorded as green because Windows real-Pi later failed only on the unrelated compact test's outer 60-second timeout.

### SES-26 — per-message Copy action

Non-empty user and assistant messages expose an accessible Copy action without requiring stable Pi entry identity; stable-entry Fork and Re-run retain their existing eligibility and semantics. User Copy writes only canonical user text, never attachment summaries or payload metadata. One assistant action writes all visible assistant text parts in order with a blank line between parts, excluding thinking, error, tool, status, and supervisor content. Hover/focus-revealed controls sit in zero-layout leading/user and trailing/assistant gutters, cannot intercept pointer input while hidden, remain keyboard reachable, preserve long/narrow wrapping, and reuse the shared success/failure feedback.

Accepted macOS browser validation passed the full workspace suite (including 684 server and 226 renderer tests), all pinned real-Pi suites (77 server and 8 pi-host), focused component coverage (23/23), history-actions Playwright (4/4), typecheck, lint, format, design-system checks, the native addon build/smoke with Rust 1.88, and the production web build. Full E2E recorded 223 passed/14 skipped; the only two failures were unrelated existing checks for installed Pi 0.83.0 versus expected 0.82.0 and the terminal keyboard shortcut. Direct 720×640 Chromium inspection covered a long narrow user bubble and trailing assistant gutter: text wrapped without horizontal clipping, controls remained outside bubble content, and no row-height movement was observed. This shared React/clipboard path applies to macOS, Windows, and Linux; Windows/Linux runtime execution was not repeated. It adds no backend, persistence, IPC, process, packaging, or sync-seam impact.

### SES-10 — exact per-model thinking levels

The session model catalog now projects pinned Pi 0.82.0's exact `getSupportedThinkingLevels(model)` result into the composer. The typed launch, WebSocket, settings, persistence, and onboarding paths consistently carry `max`; onboarding warns that Pi may clamp unsupported choices. Catalog/Pi-state load races no longer produce a false unavailable state, non-reasoning models are `off`-only, and an unexpected empty reasoning result exposes no speculative menu. The listbox supports accessible option state, Arrow/Home/End navigation, Enter/Space selection, visible focus, and focus restoration; loading and unavailable controls remain focusable for status discovery while rejecting activation.

Accepted macOS arm64 validation passed typecheck, lint, format, design-system, native build/smoke, focused picker/settings tests, all pinned real-Pi tests (**90/90**), and the SES-10 composer Playwright scenarios (**5/5**), including exact ladders, model switching/clamping, race handling, and keyboard focus. The broad workspace run recorded **1,549 passed/6 skipped/1 failed**, plus **39 passed Rust tests**; the unrelated terminal-provider attention timeout passed focused. Full E2E recorded **231 passed/14 skipped/2 failed**; the unrelated failures were the global Pi 0.84.1 versus pinned 0.82.0 Doctor check and the existing terminal shortcut. Native, web, and backend builds passed, but the aggregate bundled-runtime build could not fetch private `@a-streetcoder/skill-engine-native` (registry 404), so packaged validation was not completed. Windows, Linux, and macOS x64 were not run. This closes the session composer/catalog path only: exact-level behavior in the separate Agent editor remains under active AGT-09 where applicable, and no exact-level claim is made for the separate Loop editor surface.

### SES-17 — final runtime system-prompt audit

The latest successfully captured exact final system prompt is now retained for parent and named-agent sessions after prior hooks and memory recall. Capture uses an authenticated strict bridge, a two-second best-effort acknowledgement, and monotonic per-runtime sequence fencing. Sensitive prompt evidence persists device-locally without bumping session activity; fork and rebind clear it, while full-session duplicate preserves it. An accessible private read-only dialog exposes the audit evidence. Managed child prompt auditing remains unclaimed and deferred without a registered ID.

Accepted macOS arm64 validation passed the full workspace suite (**1,561 passed/6 skipped**), all pinned real-Pi tests (**92 passed**), and the affected MCP-focused E2E rerun. Full E2E recorded **230 passed/14 skipped/3 failed**: MCP passed focused, the terminal failure reproduced, and the global Pi-version mismatch remained unrelated. Native, web, and backend builds passed; the aggregate build was blocked by a private `@a-streetcoder/skill-engine-native` registry 404. Windows, Linux, macOS x64, and packaged execution were not run.

### SES-19 — idle Pi process parking

Idle parent and named-agent chats now release their Pi process after a configurable idle delay while retaining a stable resumable session placeholder, transcript, worktree ownership, and durable `parkedAt` evidence. Parking defaults to enabled after 10 minutes and is configurable from 1–120 minutes on the accessible Performance screen. It fails closed when resumability or idle safety cannot be proved, including pending input, retries, compaction, tools, child runs, UI/ask-user/supervisor requests, failures, and Loop-owned sessions.

Parking is distinct from terminal exit: it does not set `endedAt`, record failure, finalize retries, emit `session_exit`, close session terminals/scripts, or bump activity. Transparent wake uses a fresh Pi runtime and stream generation, seeds canonical history, restores prompt-audit sequencing, adopts the runtime before broadcasting `session_rebind`, and preserves exactly-once terminal cleanup. Timer, park/resume, delete, shutdown, failed-launch, and failed-close races restore a valid owner or fail safely. Fork, history-fork, rebind, cold-restart, and named-agent birth paths arm the same lifecycle; malformed or terminal-conflicting parked metadata is discarded.

Accepted macOS arm64 evidence includes typecheck, lint/design-system/format, **152 focused server lifecycle tests**, **8 focused renderer tests**, **79 contracts tests**, the pinned real-Pi parking scenario with ordered incremental deltas and restored two-turn history, and **3 focused Playwright scenarios** covering settings persistence/error recovery plus parked restart/wake UI. Native addon build, verification, and smoke passed after adding rustup Cargo to `PATH`. A broad workspace run stopped on an unrelated Pi-host correlation timeout that passed immediately focused (**7/7**); the user stopped the later broad-gate rerun, so this closure does not claim a final green full workspace or full E2E run. Windows, Linux, macOS x64, and packaged execution were not run. `parkedAt` is device-local runtime evidence recorded in `docs/sync-seams.md`, not sync state.

## Parity-present domain summary

<!-- prettier-ignore -->
| Domain                  | Electron behavior already present                                                                                                                                                                                                                                                                                                                                                        | Evidence                                                                                                                                       |
| ----------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| Sessions                | Create, resume, rename, delete, search, cancel, idle Pi process parking with transparent resume, whole-session duplicate, per-message user/assistant Copy, stable-entry fork/edit/rerun with durable immediate-source provenance and bounded recap, persisted launch plan, real deltas, reconnect replay/snapshot, durable provider-retry status, durable per-session attention with all-session observation and review acknowledgement, durable file and folder attachments, checkpoints.                                                                                                                 | **E:** session routes/manager, transcript pipeline, composer, session/e2e tests.                                                             |
| Providers               | API-key and OAuth discovery, search/grouping, method choice, interactive prompts, browser launch, re-authentication and credential replacement.                                                                                                                                                                                                                                          | **E:** `providers.ts`, `providerLogin.ts`, `ProvidersScreen.tsx`, provider tests.                                                              |
| Agents                  | Global/library/builtin CRUD where writable, overrides/reset, scopes, named-agent launch, model/thinking/tool/skill/MCP basics.                                                                                                                                                                                                                                                           | **E:** `scanner.ts`, `writer.ts`, `routes/resources.ts`, agent tests.                                                                          |
| Skills                  | Engine-backed collection imports materialize into the ordinary catalog. Upstream sync auto-merges non-overlapping changes and presents overlapping paths for per-file Keep Mine/Take Remote choices with stale-review refresh. Pre-0.1.5/base-less collections may require re-import or the compatibility whole-skill resolution path. | **E:** `EngineSkillStore`, `routes/resources.ts`, `SkillsScreen.tsx`, real-addon and renderer conflict tests. |
| Prompts                 | Global and library CRUD, rename, assignment, slash insertion.                                                                                                                                                                                                                                                                                                                            | **E:** `PromptsScreen.tsx`, `routes/resources.ts`, prompt tests.                                                                               |
| Instructions/extensions | Safe final-symlink rejection for project instructions; extension discovery, loading, disable, and conflict display. Composer `/` browses enabled injected commands, catalog prompts/skills, and available loops.                                                                                                                                                                          | **E:** `routes/projects.ts`, `watcher.ts`, `ExtensionsScreen.tsx`, slash-universe assemble/composer, extension tests.                          |
| Dedicated Loop engines  | Loop Bank CRUD plus native-flat Maker+Checker, Agent Pipeline, report-only Parallel Agents, Discovery/Triage, and Human Approval authoring and orchestration, with durable run evidence, real-Pi sequencing where applicable, cancellation, recovery, temporary isolation, retained worktree review, and dedicated exact-patch Apply / safe-archive Discard decisions. General Git review remains read-only. | **E:** `loops.ts`, `loopEngine.ts`, `routes/loops.ts`, `LoopsScreen.tsx`, focused server/E2E Loop tests. |
| Subagents/plans         | Fresh anonymous/named/parallel streaming children, durable generic run records with restart recovery, parent-owned read-only live/canonical child transcript inspection, same-child continuation with combined history by stable run ID and opaque Pi resume handle, app-owned immutable artifact evidence with opaque reveal, child `contact_supervisor`, blocking UI answer path, current plan snapshot. | **E:** `bridgeTools.ts`, `SessionManager.ts`, `services/sessionManager.ts`, `subagentRunStore.ts`, session routes, `ChildTranscriptDialog.tsx`, native artifact capability, desktop reveal bridge, focused unit/component/real-Pi/E2E tests. |
| Memory                  | Store, search, status/type lifecycle, secret scan, tools, launch/per-turn injection, lexical/fuzzy and optional semantic ranking.                                                                                                                                                                                                                                                        | **E:** `packages/memory`, `routes/memory.ts`, memory tests.                                                                                    |
| MCP                     | Stdio and HTTP runtime, in-app stdio/HTTP add, automatic OAuth browser return with manual fallback, global runtime pause, live tools, All Projects defaults, explicit project assignments, effective source provenance, project-over-global config resolution, scoped refresh/remove/logout, and assignment-narrowed agent allowlists.                                                                                                                                                                   | **E:** `packages/mcp`, project/session routes, `mcpTools.ts`, `McpScreen.tsx`, MCP tests.                                                      |
| Issues                  | List/filter/detail/body/comments, close, browser link, and basic Open in Pi.                                                                                                                                                                                                                                                                                                             | **E:** `routes/projects.ts`, `IssuesScreen.tsx`, issue tests.                                                                                  |
| Git/release             | Status, commit, push, AI commit message, worktree merge, version proposal, AI notes, tag/push.                                                                                                                                                                                                                                                                                           | **E:** `git.ts`, `routes/git.ts`, `routes/sessions.ts`, Git tests.                                                                             |
| Project tools           | Discovery roots, project preferences, package scripts, dev preview, Browser, PTY, file edit conflict checks, diffs/comments, editor launch, palette.                                                                                                                                                                                                                                     | **E:** project/tool services and e2e tests.                                                                                                    |
| Desktop/distribution    | Sandboxed/context-isolated shell, protected loopback backend, session-routed notifications, distinct-session macOS/Linux badges and Windows raster taskbar overlays, pinned bundled Pi, signed/notarized macOS DMG.                                                                                                                                                                                                                                                        | **E:** `apps/desktop`, `electron-builder.yml`, `release-macos.yml`.                                                                            |

# Comprehensive functional difference register

Each row is one distinct user behavior or safety difference. “Plain English” deliberately restates the technical finding for a non-expert.

## Sessions, runtime, providers, and attention

<!-- prettier-ignore -->
| ID     | Priority | Status    | Difference                 | Plain English                                                                                                                 | Why it matters                                                                          | Evidence                                                                              |
| ------ | -------- | --------- | -------------------------- | ----------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- |
| SES-14 | **P3**   | Missing   | Durable notification state | Notification timestamps/state are not stored with each session.                                                               | Duplicate suppression and audit behavior are thinner.                                   | **E:** `main.js`, app settings. **N:** `PiAgentSessionModels.swift`.                  |
| SES-18 | **P3**   | Partial   | Automatic title refresh    | Electron can generate a title, but does not keep refreshing it with conversation evolution like native.                       | Older titles can stop describing the chat.                                              | **E:** title helper/session manager. **N:** `PiSessionTitleGenerationService.swift`.  |
| SES-27 | —        | Present   | Whole-session duplicate    | Electron can copy a complete Pi session into an independent session, separately from stable-entry history Fork.               | Users can choose whole-session duplication or branch at one user turn.                  | **E:** `POST /sessions/:id/fork`, history routes, session tests.                      |
| SES-33 | **P3**   | Missing   | NeuralWatt live model sync | Electron has no live NeuralWatt catalog synchronization.                                                                      | Those model entries do not refresh from the native source.                              | **E:** no equivalent. **N:** `NeuralWattCatalogSync.swift`, tests.                    |
| SES-34 | **P3**   | Missing   | OpenAI Fast toggle         | Electron has no native-equivalent Fast mode preference for OpenAI models.                                                     | Users cannot select that provider-specific runtime mode.                                | **E:** model/settings UI. **N:** model/settings files.                                |
| SES-35 | **P3**   | Divergent | New-chat lifecycle         | Native saves a draft chat without starting Pi until resume or send; Electron creates the session and launches Pi immediately. | Opening a blank chat consumes a process earlier and gives drafts a different lifecycle. | **E:** `routes/sessions.ts`, `SessionManager.ts`. **N:** `PiAgentSessionStore.swift`. |

## Subagents and plans

<!-- prettier-ignore -->
| ID     | Priority | Status  | Difference                               | Plain English                                                                       | Why it matters                                   | Evidence                                                                 |
| ------ | -------- | ------- | ---------------------------------------- | ----------------------------------------------------------------------------------- | ------------------------------------------------ | ------------------------------------------------------------------------ |
| SUB-04 | —        | Present | Child `contact_supervisor` and UI answer | Children can report progress or block for an answer; the user can answer in the UI. | This existing path must be preserved.            | **E:** `routes/bridge.ts`, supervisor tests.                             |
| SUB-14 | **P3**   | Partial | Plan event history                       | Electron stores the current plan but not a durable history of plan changes.         | Users cannot see how the plan evolved.           | **E:** `bridgeTools.ts`, plan contracts. **N:** activity/session models. |

## Loops

Evidence corrections: the native repository contains no bundled Loop definitions, so Electron's empty initial Loop bank is not a functional difference (former LOOP-18). Native restart recovery marks active runs interrupted and retains their durable session/transcript, artifacts, worktree evidence, and checkout-recovery state; it does not rediscover arbitrary untracked operating-system processes. Electron now follows that same boundary (former LOOP-21). Explicit Discard closes the review lifecycle by atomically archiving the proven worktree inside Agent Deck's private root and pruning its Git registration; it deliberately retains the branch, artifacts, and quarantined files instead of using race-prone recursive deletion.

There are no active Loop-domain functional parity rows.

## Agents

<!-- prettier-ignore -->
| ID     | Priority | Status    | Difference                             | Plain English                                                                         | Why it matters                                                   | Evidence                                                                                  |
| ------ | -------- | --------- | -------------------------------------- | ------------------------------------------------------------------------------------- | ---------------------------------------------------------------- | ----------------------------------------------------------------------------------------- |
| AGT-09 | **P3**   | Divergent | Picker-backed validation               | Several model/tool/skill/MCP fields use looser text inputs than native pickers.       | Typos and stale names are found later.                           | **E:** `AgentEditSheet.tsx`. **N:** management views.                                     |

## Skills and repositories

All active skill and repository rows are Ale-owned; see [Ale’s active Skills and repositories section](native-functional-parity-2026-07-24-ale.md#skills-and-repositories).

## Prompts, instructions, extensions, and injected commands

<!-- prettier-ignore -->
| ID     | Priority | Status  | Difference                        | Plain English                                                               | Why it matters                                                 | Evidence                                                                               |
| ------ | -------- | ------- | --------------------------------- | --------------------------------------------------------------------------- | -------------------------------------------------------------- | -------------------------------------------------------------------------------------- |
| PRM-01 | —        | Present | Writable Prompt Library           | Prompt Library is now scanned and writable.                                 | This older gap is closed in the working snapshot.              | **E:** current paths/writer/Prompts UI.                                                |

## Memory

<!-- prettier-ignore -->
| ID     | Priority | Status    | Difference                       | Plain English                                                                               | Why it matters                                                  | Evidence                                                                            |
| ------ | -------- | --------- | -------------------------------- | ------------------------------------------------------------------------------------------- | --------------------------------------------------------------- | ----------------------------------------------------------------------------------- |
| MEM-13 | **P3**   | Partial   | Detail view                      | Electron lacks native’s full read-only metadata/body detail.                                | Reviewing provenance and status takes more effort.              | Same as MEM-12.                                                                     |
| MEM-19 | **P3**   | Partial   | Embedding near-duplicate quality | Near-duplicate prevention is not as semantically rich.                                      | Similar memories can accumulate.                                | **E:** memory store/semantic. **N:** memory store/embedder.                         |
| MEM-20 | **P2**   | Divergent | Injection timing                 | Electron injects at launch/per turn using a different lifecycle from native.                | The same conversation can receive memory at a different moment. | **E:** launch plan/memory injection tests. **N:** runner/memory store.              |

## MCP

<!-- prettier-ignore -->
| ID     | Priority | Status    | Difference                  | Plain English                                                                                       | Why it matters                                               | Evidence                                                                               |
| ------ | -------- | --------- | --------------------------- | --------------------------------------------------------------------------------------------------- | ------------------------------------------------------------ | -------------------------------------------------------------------------------------- |
| MCP-09 | **P2**   | Missing   | Read-only ownership state   | Foreign servers are not clearly protected/labeled as read-only.                                     | Users may expect edits to persist in the wrong file.         | Same as MCP-08.                                                                        |
| MCP-10 | **P3**   | Missing   | Reveal config file          | There is no action to reveal the owning config.                                                     | Manual repair is harder.                                     | Same as MCP-08.                                                                        |
| MCP-11 | **P2**   | Partial   | Broader config paths        | Native scans more standard/foreign MCP locations.                                                   | Some installed servers do not appear.                        | **E:** `mcp.ts`. **N:** `MCPForeignConfigScanner.swift`.                               |
| MCP-12 | **P2**   | Missing   | Smart paste                 | Pasting a config snippet does not populate an add form.                                             | Setup takes more manual translation.                         | **N:** `MCPConfigParser.swift`; E add form.                                            |
| MCP-13 | **P2**   | Missing   | Foreign config discovery UX | Electron lacks native’s guided import of discovered configs.                                        | Existing MCP setup is harder to reuse safely.                | **E:** MCP UI. **N:** foreign scanner/views.                                           |
| MCP-14 | **P3**   | Missing   | SSE transport               | Electron supports stdio and Streamable HTTP, not legacy SSE.                                        | SSE-only servers cannot connect.                             | **E:** MCP client/config. **N:** MCP transport/config.                                 |
| MCP-15 | **P2**   | Partial   | `cwd` field                 | Stdio working-directory configuration is not fully editable/presented.                              | Relative commands can launch in the wrong folder.            | **E:** MCP config/UI. **N:** server config/writer.                                     |
| MCP-16 | **P2**   | Partial   | HTTP headers                | Custom remote headers are not fully configurable in the UI/model.                                   | Header-authenticated servers may not work.                   | Same as MCP-15.                                                                        |
| MCP-17 | **P2**   | Partial   | Environment interpolation   | Native-style environment substitution/diagnostics are not fully matched.                            | Configs can work in one app and fail in the other.           | **E:** MCP config/client. **N:** config parser/connection.                             |
| MCP-18 | **P3**   | Partial   | Transport-specific fields   | The editor does not expose the full native server field set.                                        | Advanced servers require manual config.                      | Same as MCP-15.                                                                        |
| MCP-19 | **P3**   | Partial   | Per-tool descriptions       | Live tool names appear, but description detail is thinner.                                          | Users cannot easily understand each tool before exposure.    | **E:** `mcpTools.ts`, `McpScreen.tsx`. **N:** MCP server screen.                       |
| MCP-20 | **P2**   | Divergent | Tool exposure policy        | Electron bridge registration and native direct/extension tools produce different default universes. | The same agent can receive a different tool set.             | **E:** `mcpTools.ts`, launch filtering. **N:** MCP bridge/assignment tests.            |

## GitHub issues

All active GitHub issue rows are Ale-owned; see [Ale’s active GitHub issues section](native-functional-parity-2026-07-24-ale.md#github-issues).

## Worktrees, Git, merge, and release

There are no Andrea-owned active worktree, Git, merge, or release parity rows. Distribution work, including deferred final candidate DST-01, is Ale-owned in [Ale’s on-hold distribution section](native-functional-parity-2026-07-24-ale.md#on-hold--distribution).

## Projects, preview, terminal, files, diffs, palette, and checkpoints

There are no Andrea-owned active project, preview, terminal, files, diffs, palette, or checkpoint parity rows. CMD-03 is closed in the post-baseline closures above.

## Doctor, onboarding, settings, distribution, and analytics

<!-- prettier-ignore -->
| ID     | Priority | Status   | Difference                      | Plain English                                                                         | Why it matters                                                                              | Evidence                                                                       |
| ------ | -------- | -------- | ------------------------------- | ------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ |
| DOC-05 | **P2**   | Missing  | Warnings section                | Electron does not aggregate native-equivalent configuration warnings.                 | Problems are scattered or silent.                                                           | **E:** Runtime screens. **N:** doctor/settings views.                          |
| ONB-02 | **P2**   | Partial  | Onboarding preferences          | Some native defaults, subagent, update, and integration choices are absent.           | Users must discover settings later or cannot set them.                                      | Same as ONB-01.                                                                |
| DST-07 | —        | Present  | Signed/notarized macOS DMG      | macOS packaging uses hardened runtime, signing, notarization, and a release workflow. | This is a shipped distribution capability, not a gap.                                       | **E:** `electron-builder.yml`, `release-macos.yml`, validation script.         |
| DST-08 | —        | Present  | Packaged pinned Pi              | The app carries the pinned Pi runtime and launches it with Electron’s Node runtime.   | A packaged user normally does not need to install Pi manually.                              | **E:** `build-pi-runtime.mjs`, `main.js`, builder resources.                   |

## Platform-specific exclusions and portable targets

These native implementations are excluded from required cross-platform parity:

- Apple Foundation Models and Apple Intelligence execution.
- Image Playground avatar **generation**.
- macOS Computer Use implementations and Apple-specific brokers.
- Xcode bridge specifics and Xcode-only MCP integration.
- AppKit/SwiftUI implementation details.
- Apple Dictation implementation.

Portable outcomes remain targets:

- Importing a normal avatar image file, storing it safely, and displaying it.
- Signed application updates and safe runtime updates.
- Notifications and badges that cover all sessions and route to the correct session.
- Platform-appropriate browser, terminal, editor, project discovery, and MCP behavior.

A platform-specific implementation difference is not a gap unless the portable user outcome, persistence, or safety property is missing.

## Ordered workstreams and dependencies

This is shared historical sequencing and dependency guidance, not a cross-owner candidate list. Each parity loop selects active rows only from its own register. When work depends on another owner’s domain, coordinate the shared contract and sequencing rather than taking or editing the other owner’s row.

1. **Shared boundary — Preserve dedicated Loop engine honesty.** Keep every structure on its typed Maker+Checker, Agent Pipeline, Parallel Agents, Discovery/Triage, or Human Approval path, and retain server-side rejection of invalid configuration rather than introducing fallback.
2. **Shared boundary — Write and sync safety.** Preserve the native no-follow resource and Loop mutation boundaries plus the engine-backed collection seam. Keep 0.1.5 per-file conflict choices, stale-review rejection/refresh, automatic non-overlap merging, exact-byte outcomes, and recovery behavior behind `EngineSkillStore`.
3. **Shared boundary — Isolation safety.** Preserve held-root, identity-bound session-worktree allocation and deletion plus typed merge preflights/outcomes. Preserve the verified keep-after-merge policy and exact owned-branch cleanup on explicit session deletion; do not add an unsupported destructive age- or prefix-based scavenger.
4. **Andrea — Durable orchestration substrate.** Preserve app-owned child artifact/session ownership, retained detached parallel-child worktrees, and parent-owned reopenable child transcripts while extending the durable generic run substrate with approval ownership. Keep Loop worktree ownership separate from generic parallel-child worktrees; Loops and direct subagents should otherwise converge on this ownership model.
5. **Shared boundary — Loop artifact depth.** Preserve the completed retained-worktree review and explicit apply/discard decisions while extending the shared artifact lifecycle without collapsing the five dedicated orchestration paths.
6. **Andrea — Session input and transcript fidelity.** Preserve completed stable-entry fork/edit/rerun provenance and add versioned attachment fidelity outside that path.
7. **Andrea — Attention and runtime state.** Preserve durable failure/error and attention state, all-session observation, distinct-session platform badges, focus-gated acknowledgement, safe notification routing, and idle parking with deterministic teardown.
8. **Andrea — Subagent/control-plane completion.** Preserve stable-ID same-child continuation, declared read-first hints, app-owned artifacts, fail-closed parallel-child worktree isolation, and parent-scoped supervisor list/answer tools while adding asynchronous child control and missing agent config fields. A direct-launch sheet is not part of the audited native behavior.
9. **Shared boundary — Catalog and assignment depth.** Preserve completed agent assignment, default MCP assignment, injected-command, and avatar work while adding remaining instruction assembly audit, extension provenance, and watcher coverage.
10. **Shared boundary — MCP and issues.** Add HTTP create/edit, automatic OAuth callback, ownership/source controls, broader config discovery, advanced fields, issue replies/reopen, and structured issue context.
11. **Andrea — Memory calibration and transparency.** Match abstention gates, expose readiness/fallback, add recall cards/navigation, and add lifecycle controls before tuning lower-priority metadata.
12. **Ale — Tool depth.** Expand preview compatibility/inspection, diff scope, terminal organization, and checkpoint rerun semantics.
13. **Ale — Distribution except DST-01.** Complete independently actionable Pi-update and Windows/Linux pipeline work when the required target systems and credentials are available.
14. **Ale — Analytics decision.** Define consent, minimization, retention, offline behavior, and policy before treating analytics as implementation work.
15. **Ale — Application auto-update last (DST-01).** Return to the signed feed, publication, install, and update flow only after every other implementation-eligible active row is exhausted.

### Cross-owner coordination seams

- **Commands versus catalogs:** Andrea’s CMD-01/02/03 are closed. Ale owns active prompt and extension catalog gaps. Coordinate command identity, provenance, enablement, and refresh contracts before either owner changes shared catalog/command types, while keeping row closure in the owning register.
- **Onboarding:** Ale owns ONB-01 readiness gates; Andrea owns ONB-02 preferences and readiness/settings integration. Agree on the readiness result and preference schema before concurrent onboarding changes, then validate and close each row only in its owner’s register.
- **Running-session resources:** RES-12 is closed using the established skill, prompt, instruction, and extension catalog contracts. Future changes to those Ale-owned catalogs and Andrea-owned session refresh must continue coordinating identity, provenance, and refresh contracts without taking the other backlog.
- **Agent diagnostics:** Andrea’s AGT assignment and diagnostics rows consume Ale-owned skill and extension catalogs. Coordinate stable catalog IDs, availability/provenance states, and refresh behavior before changing shared agent validation contracts; implementation and acceptance remain owner-scoped.

## Recommended acceptance and validation

These are recommended checks for future implementation and regression coverage. They were not generally run for the original audit. LOOP-27 later passed the focused macOS arm64 validation described in the audit method. RES-01/02/03 and SKL-02 later passed the scoped cross-platform validation described there. The full E2E run still had unrelated existing failures and is not recorded as passing.

### Repository checks

- Run `pnpm typecheck`, `pnpm lint`, `pnpm format:check`, and `pnpm test` for affected Electron slices.
- Run `pnpm test:pi` for Pi launch, sessions, streaming, subagents, Loops, MCP, memory, providers, resources, or extensions.
- Run `pnpm test:e2e` for renderer/backend and Electron behavior. Main/preload changes require the real Electron Playwright path and clean shutdown assertions.
- Run focused native build/XCTest checks when native runtime behavior is the acceptance oracle.

### Critical behavior checks

- Assert Maker+Checker, Agent Pipeline, Parallel Agents, Discovery/Triage, and Human Approval retain distinct capability, ordering, outcome, cancellation, recovery, and persistence behavior; invalid configuration must fail closed without fallback.
- Cancel or crash every Loop/subagent phase. Verify processes, process groups, waiters, bridge tokens, streams, artifacts, timers, and worktrees are cleaned exactly once.
- Verify streaming remains incremental and ordered under parallel children. Bound all queues and retained output.
- Test final symlinks, symlinked ancestors, junctions/reparse points, and validation-to-write races for every create/edit/rename/delete path.
- For engine-backed collections, create multiple overlapping file changes plus a non-overlapping upstream change. Verify detail paths, reject a formerly valid stale merge ID with `409`, refresh to a new ID, apply mixed Keep Mine/Take Remote choices, and assert exact selected and automatically merged bytes. Retain compatibility coverage for pre-0.1.5/base-less imports.
- Put `SkillRepositories` or a persisted collection root behind symlinked ancestors. Clone, scan, watch, update, and delete must reject paths whose physical destination escapes the app-managed root.
- Force worktree creation failures. A session that requested isolation must not run in the primary checkout without explicit informed consent.
- Fork and rerun repeated identical messages. Verify the exact Pi entry and all original attachments are used once.
- Resume after restart and reconstruct images, files, folders, pastes, errors, attention, fork origin, plans, children, Loops, and artifacts from persisted records.
- Complete two background sessions and multiple events in one session. Badge count must equal distinct sessions awaiting review; notification click must select the source session.
- Test MCP OAuth state/PKCE, callback ownership, timeout, port cleanup, logout, token secrecy, source ownership, project assignment, and app quit.
- Calibrate memory with relevant, ambiguous, unrelated, embedding-unavailable, and secret-containing queries. Assert visible fallback and correct abstention.
- Validate release preflight for clean/dirty, ahead/behind/diverged, existing local/remote tag, push rejection, and rollback limits immediately before mutation.
- Validate the packaged macOS DMG and bundled Pi. Validate Windows/Linux packaging, native modules, PTY helpers, process-tree shutdown, signatures, and updates on those platforms when pipelines exist.

## Audit limitations and residual risks

- This is primarily a source review of the original exact Electron baseline `e2b64fa`, supplemented by the later focused LOOP-27 and SKL-15/16 macOS arm64, cross-platform RES-01/02/03 and SKL-02, and GIT-09 Windows/Linux/macOS validation described above; it is not a release qualification run.
- Watcher, symlink, native module, packaging, network-provider, and platform behavior require runtime tests.
- Except for claims with explicit cross-platform evidence above, macOS-only evidence does not validate Windows or Linux.
- Native behavior remains a reference. Security, privacy, updates, signing, analytics, and retention still require explicit product decisions where noted.
